# Create: Advanced Trains — 整理3（再構築の決定・未決定事項）

保存日：2026-09-22

## 1. この文書の位置付けと再開方法

この文書は、整理2への回答と、その後の要件整理会話で確認・承認された内容を保存する中間整理である。会話の全文転記ではなく、次の会話で仕様検討を継続するための決定記録とする。最終仕様の完成、実装開始の承認、実装完了を意味しない。

### 1.1 読む順序

1. 作業規則：[ドキュメント構成の参考.md](下書き・参考/ドキュメント構成の参考.md)。利用者の指示により、現在はこの下書きをAGENTS.mdとして適用する。
2. 本書：最新の承認済み事項、変更点、未決定事項、調査結果。
3. 対象テーマの不足点：[整理2.md](整理2/整理2.md)。本書で変更した内容は旧記述を採用しない。
4. 原回答：[第2稿への解答と補足](整理2/%5B1%5D整理2で出た不足点の解答と補足.md)。回答中の「想定」「仮称」「検討したい」は確定仕様に格上げしない。
5. 必要な場合のみ `vendor/Create-mc1.20.1-6.0.8` の該当ソース。

CATは一から再構築中である。再構築以前の実装・仕様・Phase・モデル固定の役割分担を現在の要件として復活させない。従来資料は履歴・参考として扱う。

本書に取り込んでいない整理2の問題は、解決済みとは扱わない。整理2は未処理事項の索引として引き続き必要である。原回答冒頭の完了条件の列挙は、本文の未決定事項が解消した証拠ではない。

### 1.2 状態の読み分け

- **決定**：利用者の明示的な回答または会話中の承認がある。
- **暫定方針**：まずこの方向で検討する承認があるが、調査や使い勝手を踏まえて方式を変更できる。
- **将来対応**：対応方針は合意したが、初期実装の完了条件ではない。
- **提案**：検討案であり、未承認の方式・実装詳細を含む。
- **要確認**：仕様判断、ソース調査、数理検証などが残る。
- **調査事実**：ローカルのCreate 6.0.8ソースから確認した内容。CATの実装・動作確認ではない。

具体的な性能値は、機能・責任分担・全体的な実装事項を整理した後にまとめて決める。既に合意した通常移行時間10tickは基本値として残し、最終的な数値調整で整合を確認する。

### 1.3 今回の保存範囲と検証状態

- 9月20日の保存以降に決まった時刻表・予約・終着時切替・駅名条件・復旧・通知・連結・主副マスコン・共通キー・操作権を、該当する章へ統合した。
- 最新の追加はレバーサーの前進／中立／後進、滑らかなレバーアニメーション、内部指定値を伴うHUDレバーの自動切替を認める方針。手動走行中のHUD終了で最大常用ブレーキを作動させる際は指定ノッチをNへ戻すが、時点・制動保持との接続・ATO等での例外は未決定（第11.10節）。
- 調査中の走行中レバーサー操作を、停止中・N限定などの確定仕様として補完していない。決定・提案・調査事実・利用者の実機報告を区別する。
- Java実装、ビルド、Minecraft起動、エージェントによるゲーム内試験は行っていない。利用者のSteam ’n’ Rails実機報告は第2.3節へ別記した。
- 併用信号方式は調査・検討中。共通の地上現示や未確定分岐の動作は決定したが、併用方式全体の実現性確認・最終採用は未完了。
- 次に話す事項は第15章を参照する。保存後は回答済みの基本事項を一から聞き直さない。

## 2. 列車判定・Createとの境界

### 決定

- 標準運転台とCATマスコンの併設は認めない。組立時に併設を検出したら、駅ブロックでエラーを出して組立を拒否する。表示専用としての併用も認めない。
- Train互換とは、特に他アドオンが参照する変数・関数などの維持を意図する。Createの挙動すべてを完全に維持する意味ではない。
- CAT制御対象では、CATと重複するCreateの通常の速度制御は無効化・置換が必要。個々の介入箇所はソースに基づいて設計する。
- 経路や占有の更新、駅到着の状態処理、線路上の移動と、速度を書き換える処理を区別する。

### 要確認

- 組立時の検出方法、組立後の構成検出可否。組立後の不正な併設を検出できるならエラー表示を行う方向だが、列車をどう扱うかは未決定。
- 主副・複数マスコン・連結時の基本方針は第2.1～2.3・11.9節による。組立解除／再組立、個体識別、連結アドオンとの接続・設定継承の詳細は未決定。
- 型・関数だけでなく、他アドオンが利用する値の意味・単位・更新時期の互換性。
- 衝突・脱線・線路終端などの処理は通常の速度制御と別に扱う。速度代入箇所を一律削除する決定ではない。
- `approachTargetSpeed` だけを無効にして完了としない。Navigationの直接補正、到達時停止、自然減速も確認対象。

### 2.1 主・副マスコンと列車方向（決定）

- 主・副は列車ごとの設定画面で固定割当する。例として下り方向の台を主とする。走行方向に応じて自動交換しない。
- スケジュールで使用する主・副と、その台を基準とした前進／後進を指定する。使用台の既定は主。
- 反対向きの副の後進は主の前進と同じ列車移動方向になる。主・副という名前だけで移動方向を決めない。
- 主副の役割、Create内部の編成基準方向、各台の設置向き、実際の移動方向を区別する。主副変更で内部の組立方向を変更する仕様は採用していない。
- 使用台を切り替えるだけなら列車方向は維持し、新しい台の前後表示を対応させる。反対向きの台なら前進と後進が入れ替わる。スケジュールに明示した方向設定は別に実行する。
- レバーサーは左右ではなく前後方向のレバー。最新の中立追加と調査中事項は第11.10節による。

### 2.2 最外側マスコンと連結・分離（決定／残課題）

- 同じ方向のマスコンが2台以上あれば、その向きに対して編成の最も外側の1台だけを運転に使用可能とする。連結部の内側の台は動作させない。
- 分離後は各列車の構成で判定し直し、再び外側になった台を使用可能にする。固定主副の割当と、現在使用可能かどうかは別の情報。
- 最外側は編成順序と車両内位置・向きに基づく。曲線上でワールド座標の大小や単純な直線距離を使わない。具体的な座標変換・同位置の競合規則は要設計。
- 連結時は継続側（利用者の実験で「先頭」とされた側）の時刻表を採用し、他方はアイテムとして返す。自動統合しない。分離時は継続側だけが時刻表を維持し、分離側へ元の時刻表を自動復元・複製しない。
- 変更予約も継続側のものを扱う。消失する列車側の予約を混ぜず、分離側へ複製しない。対象喪失予約の整理機能は将来対応。連結のたび一律に予約の再確定を要求する案は採用していない。
- CATは将来の連結・分離を予測したり、その予定を専用に管理したりしない。人やレッドストーン設備がまだ行っていない分離を待ち続けない。
- 既に検知した編成変更と、適用条件を満たした予約の処理が未完了なら、ATOの発車判断を先行させない。分離が先に検知された場合は、分離完了・引継ぎ先確認→予約処理→発車条件確認の順序を守る。
- 停車時に予約が適用された後で外部操作により分離した場合は、変更済み時刻表を継続側が引き継ぐ。外部イベント順をさかのぼって変更しない。
- 編成変更で主副が内側になる・重複する・別列車へ移る場合の再設定／復元、操作権・組立者・プロファイル・ATOS制限等の継承は未決定。「マスコン再有効化＝運行や設定の自動復元」ではない。
- 内側マスコンで情報閲覧だけを許すかは未決定。使用不能な台を指定した時刻表項目は、実行時にエラー停止する。

### 2.3 Steam ’n’ Railsの利用者実機報告（CAT検証ではない）

- 連結器のマーカーへ列車を配置し、レッドストーンで連結した。
- 連結では先頭と認識された側の時刻表を採用し、他方の時刻表は連結器付近へアイテムとして排出された。
- 分離後はA側の名前・時刻表が残り、B側は「Split off from: A」の趣旨の名前で時刻表なしとなった。正確な英語表記は未確認。時刻表を持つ側のみ自動運転を開始した。
- 各編成に運転台1台の逆向き配置で「向きを揃えてください」の趣旨の表示により拒否された。両向きに2台ずつ設置し、組立時と逆方向の状態で試しても同様だった。
- 組立基準方向が関係する可能性は仮説。拒否条件、先頭／継続側の判定、UUID・向き・NBT継承、変更完了通知はSteam ’n’ Rails側のソース調査待ち。CATで主を組立方向へ強制したり制約を解除したりする決定ではない。

## 3. 機能の責任分担

### 決定

| 要素 | 責任 |
| --- | --- |
| ATO・ATACS・TASCなど | 担当する判断・計算を行い、共有する状態・結果をINTEROSへ渡す |
| INTEROS | 列車情報の正本、マスコン・ATOSへの共有と要求受渡し、各機能からの通知要請の集約・宛先・送信間隔管理を担う |
| マスコン | 操作受付とHUD表示。表示情報はINTEROSから取得する。独自に列車制御計算・情報管理を引き受けない |
| ATOS | 常時動作し、INTEROS経由で情報を確認・共有し、プレイヤーによる遠隔操作要求を伝達する。現段階では自律的な運行操作を行わない |
| 共通の進路管理機能 | サーバー側で申請・占有・競合・既存許可・待機優先度を確認し、許可・待機・拒否を決める。マスコンやATOが独自に許可を確定しない |
| 地上信号 | 共通管理で確定した進路状態と先の区間条件に基づき、列車共通の現示を決める |

- ATACSはD-ATCとATSを統合する機能。信号との関係も含むが、具体的な責任境界はさらに検討する。
- 安全系の切替ではATACSを一括してON/OFFする。D-ATCとATSをプレイヤーが個別に無効化する方式は採用しない。
- 内部ではD-ATC相当とATS相当の責任を分離する方向。ATACS配下のフォルダーや各Controller名は構成イメージ・仮称であり、具体的な配置・名称は未確定。
- INTEROSは内部機能の総称で、無効化不可。独立した搭載機器名として表に出すことは現時点では想定しない。
- ATO中はATACS・TASCなど操縦に関わる機能を強制有効にする。ATOは操縦に関係しない機能の設定へ介入しない。
- 手動時の安全機能OFFは運転者の責任で許容する。ノッチ・前後切替などの基本操作機能は無効化不可とする方向だが、完全な対象一覧は未決定。

### 要確認

- 要求を検証・認可する具体的な担当、内部状態と共有状態の境界、データ一覧、同期・保存方式。
- ATO強制有効対象の完全な一覧、故障時動作、車上・地上・中央の配置。
- 実在名称かCAT独自名称かの採用判断。名称に関する権利上の問題を調査済みとは扱わない。

## 4. 通常ノッチ・ATO・TASC・EBの加減速度移行

### 決定

- 手動運転は離散ノッチ、ATO通常制御はノッチ性能の範囲内の連続的な加減速度要求を使う。
- 通常のP・N・B間は、変更時点の加減速度から目標加減速度へ線形移行する。基本移行時間は10tick。
- 移行中に別の目標へ変更された場合は、その時点の加減速度を起点に、設定時間で新目標へ移行し直す。
- ATO通常制御にも同じプロファイルの通常移行時間を使用する。加速度を瞬時に大きく切り替えない。
- ATOから同じ目標値が繰り返し通知されても移行を再開始しない。値が変わった場合だけ再開始する。
- TASCも通常ノッチ性能の範囲内の連続的な加減速度要求を使い、同じ通常移行時間・線形移行・目標更新規則を適用する。停止予測に移行時間を含める。
- EBは通常ノッチと独立したプロファイル別移行時間を持ち、発動時の加減速度からEB目標減速度へ線形移行する。
- 通常移行時間・EB移行時間はServer Configでプロファイル別に定義する。EB時間の既定値は未決定。
- ATOがEBを要求しても、手動EBと同じ性能限界に従う。瞬時に `speed = 0` として非常制動の代わりにする方式は不可。

### 要確認

- P・N・B・EBの性能値、速度依存、停止距離式、単位、勾配等の扱い。
- ATO・TASCが毎tick異なる目標を出すと毎tick移行が再開始される。追従性と停止距離への影響を検証する。
- 目標値の同一判定、速度依存で目標が変化する場合、EB保持中の目標更新。
- ノッチNは力行・能動的制動を要求せず、摩擦・空気抵抗等による自然減速を伴う。一定速度維持ではない。勾配等の数理モデルは未決定。Nだけで即時停止・停止保持とはしない。
- ノッチNとレバーサー中立は別。指定ノッチNと、安全装置等が実際に要求している制動も区別する。最新の自動レバー切替の接続は第11.10節で未決定事項を明示する。

## 5. プロファイル・種別・等級

### 決定

- 性能定義はServer Configで共通管理し、プロファイルアイテムは列車へ適用するプロファイルを指定する役割とする。
- 同じプロファイルを使う列車には共通の性能値を適用する。アイテムごと・プレイヤーごとに同名プロファイルの性能を変える方式は採用しない。
- MOD全体の設定範囲にプロファイル性能を追加する。整理2の「原則デバッグとATOSのみ」を、そのまま制限として適用しない。
- 種別はプロファイル名で表し、等級は性能に基づいて区別する。ただし最終等級は作成者が調整できる。

### 将来対応として決定

- 種類を固定せず、利用者が「準急」など独自の名称・性能のプロファイルを追加できるようにする。
- 追加はサーバー側のみ。クライアント独自定義や独自性能値を列車制御へ採用しない。
- 追加時に性能値から既定等級を自動算出し、作成者が上下に調整できる。
- 等級が自動算出か作成者指定かを区別する。
- 性能変更時は、自動算出の等級だけ再計算する。作成者指定の等級は維持する。

### 要確認・提案

- 標準プロファイルの種類、性能項目、設定範囲、等級段階数・算出式。進路裁定での等級の基本的な使い方は第10.6節で決定し、具体式・閾値は未決定。
- 調整した等級を停止距離の代用にしない。停止距離は実性能に基づいて判断する必要がある。
- 設定変更を再起動等の明確なタイミングで反映する案があるが、具体的な反映時点は未決定。
- 独自定義の形式、登録・アイテム割当、削除・未登録時の扱い、入力値検証。
- サーバー管理へ集約しても不正値・重複等の問題が自動的に消えるわけではない。

## 6. EBの発動・保持・解除

### 6.1 解除条件（決定）

- 発動原因が継続中なら解除不可。原因が解消しただけでは自動解除しない。
- 列車が停止した後にのみ解除操作を受け付ける。減速中は原因が消えてもEBを継続する。
- 条件不成立時の解除操作は拒否し、予約・保存しない。条件成立後に改めて操作する。
- 拒否時はマスコンに理由が分かる表示を出す。具体的な文言・表示形式はGUI設計で決める。
- レンチでの解除は採用しない。

| EB発動前のモード | 解除できる場所 | 追加条件 | 解除後 |
| --- | --- | --- | --- |
| ATO | マスコン解除専用ボタン、またはATOS | 停止済み・原因解消済み | ATOへ復帰。現在の信号・進路・時刻表等を確認して再開。別の発車操作は不要 |
| 手動 | マスコンのみ | 停止済み・原因解消済み、先にNへ戻してから解除ボタン | Nで手動へ復帰。発動前のP等は復元せず、再発進は運転者がPを操作 |

- ATOSから解除できるかは、EB発動前にATO運転中だったかで判断する。EBによりATOが停止中でも、この由来を失わない。
- ATO復帰は必ず即発車する意味ではない。信号停止や停車待機の条件があれば停止を続ける。

### 6.2 EB中のノッチ操作・表示（決定）

- EB作動中はN以外のノッチをグレーアウトし、マウス・キーのどちらからも選択できない。
- Nへ戻す操作は可能。ただしNにしてもEBの制動・停止保持は続く。
- レバー位置NとEB作動状態は別々に表示する。
- 手動時はNであることを確認して解除ボタンを受け付ける。N以外で押した場合は拒否し、Nへの操作を案内する。
- N操作を解除条件とするのは手動時。ATO由来EBの解除へ、手動のN操作を追加必須にする決定ではない。

### 6.3 運転者がEBへ入れる追加操作（決定）

- マウス：常用最大ブレーキ位置で一度ボタンを離し、改めてEB側へドラッグすると発動する。
- 一度のドラッグ継続や、EB位置への直接クリックでは追加操作を省略できない。
- キー：常用最大ブレーキで一度キーを離し、改めてブレーキ側へ押すと発動する。
- 押しっぱなしのリピート入力ではEBに入らない。
- 安全装置による自動EBは、この追加操作を待たずに発動する。

### 要確認

- 停止判定の閾値、EB発動条件の全一覧、複数原因の保持・解除、警報、解除認可。
- EBの判断主体の分担、EB中に操作権や運転モードが変わる場合、保存・再読込。
- 安全機能OFFと、既に発動中のEBとの関係。
- 防護無線は検討候補であり、採用決定ではない。

## 7. ATO・TASCと手動運転

### 7.1 手動介入と安全装置の設定復元（決定）

- ATO中に手動ノッチ操作が入ったら、直ちにATOを無効化し、その操作のノッチ要求を受け付ける。
- 加減速度は第4章の移行則に従う。入力を受け付けることと、加減速度を瞬時に変えることは別。
- ATO開始前の各安全装置のON/OFFを保持する。
- 手動へ切り替えたプレイヤーの設定で、次のいずれかを適用する。

| プレイヤー設定 | 動作 |
| --- | --- |
| 常に選択肢を表示（初期値） | 即座に手動へ切り替え、安全装置はONに維持。OFFへ戻る装置を警告し、ON維持か開始前設定の復元を選ぶ |
| 常に開始前設定を引き継ぐ | 選択画面を省略し、各装置をATO開始前の設定へ戻す |
| 常に安全装置を有効にする | 選択画面を省略し、ONを維持する |

- 選択設定はプレイヤー単位、実際の安全装置のON/OFFは列車単位で管理する。
- 選択待ちの間は安全装置ONを維持する。
- 選択画面をEscで閉じた場合も、安全装置ONのまま手動運転を続ける。
- 開始前から対象装置がすべてONなら、OFFへ戻ることへの警告・選択は不要と整理している。

### 7.2 ATOから手動への進路引継ぎ（決定）

- 通過中の進路は最後尾が競合範囲を抜けるまで維持する。
- 許可取得済み・進入前の進路は、選択と許可を引き継ぐ。手動切替だけでは解除しない。
- 選択済み・許可待ちの進路は、選択と申請を引き継ぎ、HUDに待機状況を表示する。
- 未選択の先の分岐は、切替後に運転手が選ぶ。引き継いだ行先だけを根拠に新たな進路を自動選択し続けない。
- 引き継いだ進路にも第10章の変更・取消・期限の規則を適用する。許可の引継ぎ自体でノッチ変更・自動発進はしない。

### 7.3 手動からATOへの進路引継ぎ（決定）

- 通過中の進路は変更せず、最後尾の競合範囲通過まで保護する。その先からATOの運行を継続できるか確認する。
- 選択済みの進路がATOの運行経路と一致する場合は、選択・許可・申請を引き継ぐ。
- 一致しない場合はATOへの切替を拒否し、HUDに理由を表示する。有効化操作だけで選択済み進路を自動変更しない。
- 未選択ならATOが運行経路に従って選択・申請する。ただし必要な行先・経路が決まっていなければ切替を拒否し案内する。
- 切替不成立なら手動運転と現在の進路状態を維持する。

### 7.4 走行中のATO有効化（決定）

- 停止中に限定せず、行先・運行経路・進路整合性などの作動条件を満たせば走行中も有効化できる。
- 現在の速度と実際の加減速度を引き継ぐ。切替で速度を瞬時に変更しない。
- 通常指令にはプロファイル別の通常移行時間を適用する。安全装置の介入が必要なら、その制御を優先する。
- EB作動中の新規ATO有効化は拒否する。ATO有効化をEB解除手段にしない。
- ATO由来EBを正規に解除した後のATO復帰は、第6章の別の復帰経路として維持する。

### 要確認

- HUD閲覧・操縦担当取得だけではATOを解除しないことは決定済み（第11.9節）。ノッチ以外の切替入力の完全な一覧、安全設定の復元とキー取出し等の関係は残課題。
- 安全装置が既に介入中の場合、装置OFFへの復元をどう扱うか。

### 7.5 手動運転のTASC対象駅（決定）

- 手動運転では、行先として指定した次の停車予定駅をTASCの対象とする。「現在地→A→B」でB指定なら、途中のAを対象にしない。
- 行先未指定時の最短駅・複数駅のHUD表示は案内のみとし、表示されているだけではTASC対象にしない。停車支援を使う場合は対象駅を指定する。
- 行先指定だけでTASCをONにしない。TASCがOFFなら駅情報の案内だけを行う。有効な信号等の安全制御は別に継続する。

### 7.6 通常補助とボタン操作による自動停止（決定）

- 通常は運転手が操縦し、TASCが減速の不足・過大を補正する。手動主体の操作イメージを山手線型とするが、以下はCAT独自の制御仕様であり実車を完全再現する決定ではない。
- 完全な自動停止は専用ボタン操作で利用する。減速・停止をTASCに任せるモードであり、駅間運行・次駅への発車までATOに切り替えるものではない。
- 自動停止中に手動ノッチ操作が入ったら、自動停止モードを終了し、TASCをONのまま通常補助へ戻して、その入力を受け付ける。補助の可変上下限と通常移行則は適用される。
- 通常補助の正確な開始位置・条件、ボタンの受付範囲・制御開始時点は未決定。初期減速まで任せる自動停止と、手動主体の通常補助を混同しない。

### 7.7 可変上下限による通常補助（決定）

- P5～P1・N・B1～B7に相当するプロファイルの通常性能範囲内で、力行・惰行・制動を自動調整できる。離散ノッチを自動選択する方式ではなく、ATOと同様に中間の加減速度も要求できる。
- ブレーキの最小・最大は、符号付き加速度ではなくブレーキの強さを表す。
- 最小ブレーキ：これより弱いと停止目標をオーバーランするおそれがある下限。
- 最大ブレーキ：これより強いと目標より手前で止まりすぎるおそれがある上限。
- 上下限は定数にせず、残り距離・現在速度・実際の加減速度・列車性能・移行時間などに応じて変化させる。
- 手動要求が最小より弱ければ最小まで強め、最大より強ければ最大まで弱め、範囲内なら手動要求を採用する。B7を指定しても、その時点の最大ブレーキを超える場合は最大まで弱める。「B7を最小へ合わせる」という符号混同の初期表現は採用しない。
- EBはこの通常調整範囲に含めない。有効な安全制約・安全装置の制動要求をTASCが弱めたり上書きしたりしない。
- 第4章の通常移行時間（基本10tick、プロファイル別設定）を適用する。連続指令更新時の追従性を含む数式・性能検証は未完了。

### 7.8 TASCの無効化（決定）

- 意図的に手前で停止するなど補助を不要とする場合、TASCをOFFにして通常の手動操作へ戻す。
- 機能OFFと一時的な補助無効化に別フラグを用意しない。同じTASC有効／無効状態を操作する。
- 手動運転中は停止・駅通過だけで自動的にONへ戻さず、運転手が再度ONにする。第3章のATO作動中の強制有効とは区別する。
- 自動停止／通常補助というモードの区別は、無効化フラグの二重化を意味しない。

### 7.9 駅と信号の責任・非常制御（決定）

- 停車予定駅の停止目標に対するオーバーランはTASCの責任。停止信号の冒進・オーバーランはATACS配下のATSの責任とする。その他の制限に対する内部の詳細分担は未決定。
- 最大常用ブレーキB7相当でも停止目標超過を避けられないと予測したら、TASCがEBを要求する。通常補助の「かけすぎ防止」の上限は非常制御を妨げない。
- 発動後は第6章の共通EB処理とEB専用移行時間に従う。瞬間停止は行わず、EBでも停止目標を超過する可能性はある。
- 複数原因があれば、駅側の原因だけの解消で信号等の原因まで解除しない。
- 駅手前の停止信号について、有効なATSの停止制約をTASCの力行・制動緩和より優先する。TASCの対象駅と動作モードは保持する。
- 通行可能となり信号・進入許可等の条件が整えば、元のモードへ戻る。自動停止モードならTASCによる加速・減速を再開し、通常補助なら運転手の操作＋TASC補助へ戻る。
- EBが発動した場合は、現示が変わっただけで解除・再開しない。通常のEB解除条件・操作を適用する。

### 7.10 停車完了（決定）

1. 所定の停止位置の許容範囲内にいる。
2. 列車が停止している。
3. 手動レバーがNまたはBノッチにある。

- 手動運転中の通常補助・ボタン自動停止では、上記を満たして停車完了とする。許容範囲と停止閾値の具体値は未決定。
- 位置・停止の条件を満たしてもPノッチが残っている間は、TASCが停止保持し、HUDでNまたはBへ戻すよう案内する。この場面で自動Nへ戻す従来案は不採用だった。最新方針で自動レバー切替自体は可能になったが、TASC停車完了へ適用するかは未決定であり、自動Nを追加したとは扱わない。
- 運転手がNまたはBへ戻して条件がそろったら、その駅への補助・自動停止を終了し、現在のノッチの通常動作へ戻る。TASC機能自体はONのままにする。
- 次の停車予定駅は手動で指定する。HUD上の最寄り駅を勝手に次の対象へ設定しない。
- 停止信号による手前停止、許容範囲より手前の停止は停車完了と扱わず、対象駅を保持する。有効な信号制約やEBを停車完了で解除しない。
- ATO運転中の停車完了と時刻表進行の関係は別途整理する。手動レバー条件をそのままATOへ追加する決定ではない。

### 7.11 オーバーラン後の後退修正（決定）

手動運転中のTASCで行き過ぎた場合は、元の停止目標を保持して次の流れで修正する。

1. EBで停止し、既存の条件・通常の操作で非常状態を解除する。
2. 運転手が進行方向を「後」にする。
3. Pノッチで後退を開始する。
4. 停止位置からの誤差表示を見ながら、手動＋通常TASC補助、またはボタン操作による自動停止で位置を合わせる。
5. 許容範囲内で停止し、NまたはBへ戻して停車完了とする。

- 駅オーバーラン専用の解除条件、停車失敗の追加確認、停車目標取消操作は設けない。手動由来EBは停止後にNへ戻し、継続中の別原因がなければ通常の解除ボタンで解除する。
- 既に行き過ぎたという事実だけを理由に解除を拒否し続けない。解除後の修正走行で新たな非常条件が成立すれば再度EBを発動する。
- 解除だけで自動発進・自動後退しない。元の停止目標を取り消す案、解除操作に特別な確認条件を加える案は採用しない。
- 後退でも通常の補助・自動停止方式を使う。合わせるのは最初に停車しようとした列車位置であり、後退で編成の反対側が先頭になったことを理由に、その反対側を元の目標へ合わせない。
- 後退中も有効な信号・進路の制約を優先する。方向別の信号対応、後退時の進路再評価、距離・停止基準の具体的な実装は未決定。

### 7.12 手前停止からの再開（決定）

- 許容範囲より手前で停止しても停車完了とせず、対象駅を保持する。
- 通常補助では、N・Bのまま自動発進しない。運転手がPを操作して進み、TASCが補助する。
- ボタン自動停止では、信号・進路などの条件が整えば、TASCが加速・減速して停止目標へ移動できる。
- EBが発動していれば、先に通常の解除条件と解除操作を満たす。

### 7.13 TASC対象駅の変更（決定）

- TASCがONでも、補助・自動停止の制御開始前なら対象駅を変更できる。
- 制御中の対象変更は受け付けず、先にTASCをOFFにするよう案内する。
- OFFにして対象を変更し、再度ONにした時点で、経路・距離・停止可能性を再評価する。ボタン自動停止には改めてボタン操作が必要。
- TASCをOFFにしても、EBや他の有効な安全制約は解除しない。

### 7.14 停止目標の共通化（決定）

- 同じ列車・駅・進入経路について、ATO、通常TASC補助、ボタン自動停止で同じ停止目標と列車側の基準を使う。HUDの距離・停止誤差もこれに合わせる。
- 運転モードの切替や後退修正だけを理由に停止目標を移動しない。別ホーム・別の進入経路では別の目標となる場合がある。
- 駅側のどの位置と列車側のどの点を対応させるかは未決定。Createの停止処理との接続も調査が必要。

### 7.15 停止目標喪失とATOの再発車承認（決定）

- 駅・線路の破壊や情報取得不能で対象を利用できなくなった場合、通常の手動＋TASCは補助を中断し、対象が利用できない旨を表示して、手動操作と残る安全制御へ戻る。
- ボタン自動停止・ATOでは停止に向けて減速し、別の駅を勝手に代替目標にしない。ATOの実際の目標喪失はエラーとする。一時的な情報不足は第7.17節の情報確認待ちと区別する。
- 対象を利用できないこととTASC機能OFFは区別し、有効フラグは維持する。情報不明と実際の削除も区別する。既存のEB・信号制約は維持する。
- 同名駅の再作成や経路の再発見だけではATOを自動再発車させない。ATOSまたはマスコンで再発車を承認し、INTEROS経由で処理する。
- 再開時は対象駅・経路・信号・進入許可・安全条件を再確認する。承認はその復旧対象駅に対するもので、別の駅へ使い回さない。待機・処理中に対象が変わった場合は古い承認で実行しない。
- この承認は目標喪失エラーからの再開に必要なもの。通常の信号待ちやすべてのEB解除に追加の発車操作を要求する変更ではない。EB解除が必要なら、その条件も別に満たす。
- 同名駅の再作成で自動復帰するCreate標準動作は利用者からの説明であり、今回その動作をソース検証したという意味ではない。CATは上記の承認方式を採用する。

### 7.16 ATO開始・復旧承認・無人運転（決定）

- ATO開始時は時刻表全体を表示し、実行開始項目を確認・変更できる。現在の実行項目を初期選択し、新規適用なら指定開始項目、指定がなければ先頭項目を使う。近い駅へ自動で飛ばさない。
- ATOボタンによる開始操作が実行位置の確定と、必要な再発車承認を兼ねる。承認と再開を二度押させない。ATOSの再発車承認も車上へのATO再開要求であり、現地プレイヤーの追加承認を求めない。
- エラー復旧は原則、失敗した項目から再実行し、完了済み項目をやり直さない。開始項目を明示変更した場合はその指定に従う。
- 承認は時刻表・実行項目・具体的な駅・経路に対応する。承認待ちに対象が変われば古い承認を使わず、最新対象で再承認する。ワイルドカード全体への包括承認ではない。
- エラー復旧時は「設定方向のまま目的地へ至る経路が成立するか」と「今すぐ発車してよいか」を分ける。経路は成立しても赤信号・進路許可待ちならATOへ復帰して停止待機できる。経路を確認できないまま走行を再開しない。
- EB解除は兼ねない。必要なら先に通常の解除を行う。通常のATO由来EBや信号待ちに、目標喪失用の追加発車操作を一律追加しない。
- 運転手あり／なしとATO ON／OFFを区別する。無人ATOは遠隔承認だけで通常のエラーから復旧可能。エラーでのATO中断と、手動へのATO OFFを混同しない。
- 「運転手なし・ATO OFFは必ず完全停止」という初期表現は、HUD終了ノッチにNを選べる最新方針により一般保証ではなくなった。惰行中の無担当状態もあり得る。無人状態を理由に一律の別制動を追加しない（切断・キー取出し等の個別規則は第11章）。
- 現在項目が実行不能ならエラー停止し、項目を飛ばしたり別操作へ置換したりしない。後続項目だけの問題で現在の正常運行を停止しない。入力・保存時の事前案内は採用未確定の候補であり、必須ではない。
- 項目が空なら警告してATOを開始不可とする。手動運転は可能。切替先が空の場合も勝手に旧時刻表へ戻さない。
- ATOの有効化・解除が成立した際は、音またはメッセージで即時通知する。手動介入・時刻表取出し等も対象。INTEROS経由とし、長時間待機の定期送信とは別。失敗時は有効化通知ではなく理由表示。音／文字の構成・全宛先一覧は未決定。

### 7.17 予定外の後退退避・情報確認待ち（決定）

- 線路喪失等で前方へ脱出できず、予定外の後退が必要なら自動反転せず、手動退避を要求する。無人ATOもこの場合は現地対応が必要。退避後も経路発見だけでATOを再開せず明示操作する。
- 手動退避前に他列車の進入側の信号を遠隔／現地操作で停止にするのは、マニュアル上の推奨手順であり必須条件ではない。信号操作の具体GUI・権限・解除方法は未決定。
- スケジュールに明示された予定どおりの折返しは手動退避とは別で、自動実行できる（第8.8節）。
- 一時的な情報不足は「情報確認待ち」とし、安全に進行できないなら停止・待機する。元の目標・経路・許可・信号の有効性を再確認できれば、ATOは追加承認なしで再開する。
- 時間経過だけで駅・線路の喪失と判定しない。実際の喪失を確認したらエラー復旧へ移る。長時間の情報確認待ちは第8.15節の通知対象。EBがあれば情報回復だけで解除しない。

### TASC・目標喪失の残課題

- 駅と列車の具体的な停止基準点、編成長・前後方向との対応、許容範囲、制御開始条件。対象変更の基本的な受付規則は第7.13節で決定済み。
- 可変ブレーキ上下限の算出、力行側の許容範囲、移行時間を含む停止予測、予測誤差・更新周期とEB判定余裕。
- TASCのOFF・対象喪失時の制御出力の引継ぎ、残ったP入力の扱い、信号情報欠損時の具体処理。途中停止からの基本的な再開規則は第7.12節で決定済み。
- 停車完了後の対象駅のHUD表示・選択状態、逆方向修正中の表示と入力、ATO運転との統合。

- 駅指定は名前、承認は具体駅・経路に結び付ける基本は決定済み。対象喪失後のボタン自動停止の復帰条件、停止前の再発車承認の受付可否、具体的な識別／版管理は残課題。
- ATOのOFF／ON・手動介入・時刻表の取出し等とエラー保持・承認待ちの関係。承認処理を意図せず迂回しない状態遷移の整理が必要。

## 8. ATOSの権限・制限速度・時刻表編集

### 8.1 指令と車上判断（決定）

- ATOSの運行上の要求はATOの通常運転判断より優先する。
- 有効な安全制約やEB解除条件はATOSでも上書きしない。
- 現時点ではATOSがノッチを指定したり、進行方向を直接操作したりする設計にはしない。将来必要になれば再検討する。
- 車上の担当機能が要求の実行可否を判断し、INTEROS経由で結果と理由をATOSへ返す。
- 目標喪失エラーからの再発車承認と基本的な時刻表編集は第7.15・8.6・8.7節で範囲を決定した。その他の要求一覧・全受付条件は未確定。

### 8.2 手動運転での速度制限（決定）

- 制限速度は運転上の基準であり、それだけで手動ノッチの加速を強制的に抑えない。
- 安全装置OFFなら、制限を超えてもPノッチを続ければ列車の性能限界まで加速できる。基準まで減速するかは運転者の判断。
- 安全装置ONなら、各機能が自分の責任に従って警告・運転入力の一時無効化・制動等へ介入する。具体的な介入条件は未決定。
- ATOSの制限速度も、ATACS内部の安全機能が介入判断に使う入力へ含める。
- 同じ場所・時点に適用される有効な速度制限は最小値を採用する。一つを緩和しても、他の制限は残る。
- 性能限界、運転基準の制限速度、安全装置の介入条件を混同しない。
- 先の地点の制限と、現在地点へ換算した減速制約は区別する。先の低い速度を即座に現在の速度へ代入する決定ではない。

### 8.3 適用範囲と永続化（決定）

- 初期段階のATOS制限は列車単位で管理できればよい。
- 将来は区間・エリア単位へ拡張できるよう、大幅な変更を必要としない構造にする。
- 明示的な変更・解除まで制限を保持する。ATO／手動の切替、保存・再読込でも維持する。
- 設計案：制限の発生元・適用対象・値・識別子を持ち、適用判定と最小値統合を分離する。具体的な型やAPIは未決定。

### 8.4 列車からのATOS制限解除要求（決定）

1. マスコンからINTEROSを通じて、対象のATOS制限の解除を要求する。
2. 要求中は制限を維持する。
3. ATOS側操作者が承認または却下する。
4. 承認時は要求対象と現在の制限を照合し、一致するものだけ解除する。
5. 結果をマスコンへ返す。他のATOS制限や信号等の制限は変更しない。

- 各要求と対象制限を確実に識別する。要求中に対象が変更・置換・解除されていたら、解除処理を行わない。
- 同じ数値へ戻った場合でも、別の更新であることを検出できる必要がある。
- 要求ID・制限ID・版の組合せは提案された実現方法。識別保証が要件であり、具体的なデータ形式は未確定。
- 要求が古くなった場合のUI文言、再要求、重複承認、期限、要求者・承認者の権限は未決定。
- この要求機能はATOS速度制限の解除であり、EB解除要求とは別の機能。

### 8.5 常時稼働と自動操作の範囲（決定）

- ATOSは導入あり／なしで動作を切り替える機能ではなく、常時動作する。プレイヤーが遠隔操作設備・画面を利用していなくても本体は動作する。
- 情報の集約・共有と操作要求の伝達は常にINTEROS経由とする。ATOSから車上の各機能へ直接操作する経路を設けない。
- 現段階ではATOSが自分の判断で速度調整、臨時制限、発車時刻変更などの運行操作を行わない。遠隔操作要求はプレイヤーの操作に基づく。
- ATOの自動運転、安全装置の介入、共通の進路管理による許可判定は各機能が担う。ATOSによる自律操作とは区別する。
- 遠隔操作がないことは、既に設定済みの制限を消す意味ではない。第8.3節の保持規則を維持する。
- 将来の拡張候補：遅延時の発車時刻・停車時間などの自動調整。初期実装には含めず、何をどの条件で短縮・変更するか、権限、適用範囲は未決定。
- プレイヤーが事前登録した変更予約・終着時切替の条件成立時の自動実行は対応する。ATOSが自分でダイヤを最適化する自律機能とは区別する。

### 8.6 基本的な遠隔時刻表編集（初期範囲として決定）

- ATOSで列車に適用中の時刻表を閲覧し、停車予定の追加・削除・並べ替え、対象駅の変更、対応する待機条件・時刻設定の編集を行えるようにする。
- 編集途中の操作を逐次列車へ適用せず、確定操作でまとめて適用する。
- INTEROS経由で変更要求を検証・適用し、結果をATOSへ返す。プレイヤーによる編集であり、ATOSの自律的なダイヤ調整ではない。
- 対象はCATで対応を決めた時刻表命令に限る。Createの全命令への対応を承認したものではなく、対応命令一覧は未決定。
- 走行中の下書き・予約、適用時点の実行位置、待機の基本は第8.8節以降で決定。対応命令の全一覧、時刻表アイテムと車上データの正本・整合、同時編集・遠隔認可は未決定。

### 8.7 エラー中の時刻表変更と再開（決定）

- 例としてA→B→Cの運行中、破壊されたBが原因のエラーでBの停車予定を削除した場合、変更後の時刻表を再評価する。Bに関するエラーが解消しても、編集だけで自動発車しない。
- 新しい実行対象・駅・経路を確認し、編集とは別にATOSまたはマスコンから再発車承認を受ける。遠隔編集と、マスコンから時刻表を取り出して編集・再挿入した場合に同じ方針を適用する。第7.16節によりATO開始操作が承認を兼ね、二つの開始ボタンを要求しない。
- Bへの古い承認をCへ使わない。承認は変更後の時刻表・現在の実行項目・対象駅・経路と対応付ける。具体的なID・版管理方式は未決定。
- 次に実行できる項目・対象がなければ停止を維持し、勝手に目的地を作らない。
- 再発車承認でも他の安全条件やEB解除条件は上書きしない。

### 8.8 時刻表GUI・直接操作・方向設定命令（決定）

- GUIはCreate標準の編集画面のように全運行項目を表示し、開始項目の指定も同じ画面で行う。開始指定を変更していない場合と、明示変更した場合を区別する。
- 明示変更なしなら適用時点の実行位置・到着済み状態を引き継ぐ。予約を作った時点へ巻き戻さない。BからDへ開始指定を移せば、適用時の現在地からDへ向かう。
- HUDから時刻表を取り出してプレイヤーのインベントリへアイテムとして戻し、編集・再挿入できる。遠隔予約とは別の直接操作で、指定開始項目からATO開始操作で再開する。エラー中は既定の承認を兼ねる。
- ATO中に時刻表を取り出したらATOを解除し、手動の指定ノッチをNへ変更して自然減速へ移行する。HUDを閉じた場合は第11.4節。インベントリ満杯時に拒否する案は提示済みだが未確定。
- 方向設定命令は「主／副マスコンを使用し、前進／後進に設定」とする。「反転する」命令ではなく同じ指令の再処理で二重反転しない指定。停止中に実行し、命令自体では力行・発車しない。
- 次の待機・移動項目を順に処理し、方向別の経路・信号・許可を確認する。使用不能なマスコン・必要なキー欠如などは実行時エラー。別の台へ自動置換しない。
- 最新の手動レバーサー中立追加をスケジュール命令へどこまで広げるか、未完了命令の保存形式等は未決定。

### 8.9 待機時間・終了・繰返し（決定）

- 経過時間の待機はサーバーtick基準。通常20tick/秒の換算で、サーバー停止中の現実時間は加算しない。保存した経過分から再開する。時刻指定待機の時系・対応範囲は別途未決定。
- 駅停車中は信号・進路待ちと並行計測する。時間を満たしたら達成済みを保持し、信号が進行になってから数え直さない。発車は全条件成立後。
- 手動に切り替えたら時刻表の計時を一時停止する。手動の発車は時刻表の待ち時間で制限せず、運転手判断で行える。有効な安全制約は別。
- 同じ停車・元の待機項目でATOへ戻すなら保存した経過時間から計測再開。手動中の停車時間は加算しない。30秒中10秒で手動へ移り、15秒後に戻しても残り20秒。
- 手動で出発した後は残時間を走行中に消化したり元の駅へ勝手に戻したりせず、ATO開始GUIで実行項目を確認・指定する。
- 繰返しありは先頭へ戻る。繰返しなしは最終項目完了で終着・運行完了とし、新しい時刻表がなければATOは次の運行を始めない。エラーでも自動ATO OFFでもなく、停止保持・安全機能は維持する。

### 8.10 遠隔の変更予約と適用時点（決定）

- 現在の運行に影響しない後続項目は走行中も編集・反映可能。実行中項目への変更は、走行中には即時反映せず変更予定要求として予約できる。下書きと確定・適用を分ける。
- 通常の予約適用は駅停車完了時だけ。信号待ち・手前停止・EB停止では適用しない。対象駅を指定する予約では、予約確定後の最初の該当停車で一度だけ適用する。
- 対象駅に停車完了済みで未発車なら、その停車で適用できる。少しでも発車済みなら次回へ持ち越す。駅付近での再停止だけで同じ停車へ戻さない。数値上の発車閾値は後で決める。
- 予約対象は現時点で存在する駅に限定し、現在適用中の時刻表の実行位置以降に停車が見込めることを確認する。繰返しは次周回も含むが、終着時に切り替える別時刻表は追わない。通過のみ・今後停車なしなら理由を示して拒否する。
- 予約適用時の状態を引き継ぐ。開始項目を変更した場合は指定項目へ移るが、現在の停車の残り待ち時間は既定で維持する。予約時に「時間待ちを省略」を選べる。信号・安全条件・必要な再発車承認・時間以外の条件は省略対象外。
- 駅喪失で対象まで進めない場合は、停止後に変更を受け付ける。既存予約は自動適用せず、内容を確認して明示的に適用し、ATO再開承認を経る。
- 適用後に経路が見つからなければ変更済み時刻表を保持してエラー停止する。旧時刻表へ自動復帰しない。

### 8.11 予約の置換・取消・破棄（決定）

- 変更待ち予約は列車ごとに1件。再確定で丸ごと置換し、既存予約を置き換える旨を通知レベルの注意書きで表示する。適用前は取消可能で、取消だけでは現行時刻表を変えない。
- 予約後に現在適用中の時刻表の内容、または実行開始項目を明示編集したら予約を破棄し、対象・理由を通知する。「再確認待ちで内容を保持する」という旧案は不採用へ変更。
- 通常運行による実行位置更新、画面を開くだけ、編集取消では破棄しない。予約自体の再編集は置換。将来の終着時切替用コピーの編集でも現行時刻表が変わらなければ予約は破棄しない。
- 駅改名・削除で予約条件に一致する駅がなくなっても、時刻表編集とは区別して予約と名前条件を保持する。一致する駅がない旨を表示し、新設駅が条件に一致すれば再び適用対象にできる。ATOエラーの再開承認は別。
- 一括更新の一部の列車だけが編集されたら、その列車の予約だけを破棄して一覧へ反映する。
- 破棄によって競合していた終着時切替があれば、元の切替設定を適用対象へ戻す。破棄自体で即時切替・発車せず、元の切替条件を満たしてから実行する。

### 8.12 終着時の時刻表切替（決定）

- 駅ブロックからの自動コピーに頼らず、ATOSに事前登録する終着時切替で対応する。CAT列車の自動適用入口はATOS経由とし、マスコンへの直接挿入は別に認める。標準Create列車の動作まで変更する決定ではない。
- 切替先は登録時の時刻表のコピーを保持する。原本の編集を黙って追従しない。更新は明示操作する。
- 終着駅の停車と最終待機条件の完了後に適用する。切替先を登録・編集しても、それ以前の適用中時刻表や走行を変更しない。
- 切替後の開始項目が現在停車駅に一致すれば到着済みとし、入り直さない。新しい時刻表の待機条件は新規開始する。往路末尾30秒＋復路先頭30秒なら両方待つ。変更予約の残時間継承とは別。
- 切替設定はプレイヤーが削除するまで繰り返し有効。一回の実行では消さず、不要予約の自動削除にも含めない。往路完了→復路、復路完了→往路を事前登録できる。
- 設定なし／フィルターで対象なしなら指定なしとして終着で停止し、新時刻表を待つ。通常の運行完了であり、エラー・長時間待機通知の対象ではない。
- 最優先設定の適用後に駅・経路が見つからなければエラー停止し、低順位の別設定へ自動で代替しない。復旧後は再発車承認を必要とする。

### 8.13 切替設定のフィルター・競合・一括更新（決定）

- 終着時切替は列車名フィルター（完全一致／`*`）・除外フィルター・明示的優先順位を持つ。対象と除外の両方に一致したら除外を優先する。
- 切替条件と列車名条件を満たし、除外されなかった設定の最優先1件を適用する。同じ最上位順位が複数なら停止したまま通知し、プレイヤーがその回の採用先を選ぶ。無人列車もATOSから選択できる。
- 選択は今回だけに有効で、設定の優先順位・除外条件は自動変更しない。選択待ちに候補・切替先・列車名等が変わったら、古い選択で適用せず、最新内容で選び直す。
- 追加・編集時に現在確認できる重複と優先対象を警告し、実際の切替時にも再判定する。列車名の変更・列車追加で後から重複する可能性がある。
- 一度限りの変更予約は終着時切替より優先する。同じ停車で予約を適用したら、旧時刻表に基づくその回の終着時切替を重ねて実行しない。永続設定そのものは削除しない。
- 予約と終着時切替のどちらを追加する場合も、列車・駅・競合内容・優先適用先・今回実行しない側を警告する。優先予約が有効なまま適用不能なら停止し、黙って下位へ進まない。予約破棄・将来の自動削除なら元の設定へ戻す。
- ATOSで対象列車を複数選んで一括更新でき、各列車に「○○駅の停車完了時に適用」を指定できる。要求をまとめることと、同時適用することは別。
- 列車ごとに適用済み／待機／破棄／エラー等を表示する。既存予約の置換は事前通知。一部の失敗だけで他列車の成功を取り消さない。遠隔操作権・同時編集の詳細は未決定。

### 8.14 不要予約の整理（将来機能、最初期には含めない）

- 連結で対象列車を失う、処理済み予約が異常に残る等の蓄積に備えた整理・削除を将来追加する。通常の1件管理・置換・取消・適用完了処理は初期から行う。
- 一致する駅が存在しないと確認された状態が一定時間続いた場合、削除前に通知し、猶予後も復旧しなければ予約を自動削除する。削除前に一致駅が復旧・新設されたら削除計時を解除して通常待機へ戻す。
- 単なる情報取得不能、満線、信号待ちを駅不存在とみなして削除しない。正当な長期予約を経過時間だけで一律削除しない。
- 自動削除後は元の終着時切替設定を再び適用対象とし、既定の条件成立を待つ。永続の終着時切替設定そのものは自動削除対象外。
- 保持期間・予告猶予・履歴・対象喪失列車の宛先保持等は未決定。通知はINTEROS経由、オフライン未配信保存なしの既定方針に従う。

### 8.15 INTEROSによる通知（決定）

- 信号停止等を除く長時間待機をチャットで知らせる。正常な終着・指定なしの運行完了は対象外。通知対象理由の完全な一覧・具体的な文言は未決定。
- 各機能は理由の発生・更新・解消と通知要請をINTEROSへ渡す。Minecraftへ直接送信せず、各機能独自に再通知タイマーを持たない。
- INTEROSが列車単位で要請をまとめ、通知対象の複数理由を1件に併記する。長時間待機判定時間と再通知間隔は別に管理し、具体値は全体整理後に決める。
- 送信間隔はINTEROSが独立管理する。同じ理由の更新や新規要請で送信を前倒し・先送り・計時リセットしない。新理由は自身の通知条件成立後に次の送信へ含める。
- 対象理由が残れば定期送信し、全部解消すれば終了する。信号待ち等の対象外理由だけが残った場合も終了する。
- 通知先は列車の組立者を基本とし、一部の通知は現在の運転手にも送る。運転手とは有効なマスコンで操縦担当を取得したプレイヤーで、乗客・閲覧者ではない。同一人物へ二重送信しない。
- 共通キーに所有者を持たせないため「キー所有者と組立者へ通知する」という以前の案は廃止。ATO切替・運転エラー・選択要求等は運転手向け候補だが、宛先の完全な対応表は未決定。
- 各送信時にオンラインの対象者へ送る。未配信を保存・まとめ配信しない。ログイン時も待機が続いていれば次の定期送信で届く。
- 重大度による即時通知・間隔例外は別の検討項目。今回の長時間待機では要請追加を重大度変化とみなさない。ATO切替の即時通知、キー取り外し忘れ音は、それぞれ決定済みの個別通知。

## 9. 信号・距離併用方式の検討状態

### 決定した方向

- 地上の信号現示は列車ごとに変えず、共通とする。
- 列車性能ごとの減速開始位置や安全介入条件の違いは車上側で扱う。
- 制動性能・移行時間が異なれば、同速度・同地点でも必要停止距離が異なる。等級の表示だけで判断しない。

### 併用方式全体は検討中

- 候補：区間の占有・進路予約で「入ってよいか」を管理し、実距離と列車性能で「減速・停止が間に合うか」を判断する。
- 利点：信号間隔の違い、プロファイル差、ATOS制限を扱いやすい。運行許可と運動制御を分担できる。
- 最終採用の前に、第10章で決めた進路管理をCreateへ接続する方法、処理順、距離精度、性能・情報欠損を評価する。予約の判断主体はサーバー側の共通管理と決めたが、実装方式は未決定。
- 固定の「停止→停止→警戒→注意→減速→進行」という配列は再確定していない。
- 現示ごとの速度値、停止区間を追加する具体的意味、地上現示と車上制限の表示の関係は未決定。
- 「遅い列車にEB、速い列車を進行」という旧案を確定ルールとして採用しない。予約・等級等を統括した裁定を検討する。

### 信号現示・進入許可・事故の責任範囲（決定）

- 進入許可とは信号を進行現示へ変更するだけの許可ではなく、列車が通る進路を競合なく確保する許可である。
- 申請後、競合する新規進入を防ぎ、占有や既存許可を確認して進路を確保し、その状態を信号現示へ反映する。許可が出ても先の条件によっては速度を抑える現示となり、必ず進行現示になるわけではない。
- 例：駅Aの停車線から本線へ合流する普通列車を先に出すなら、競合する快速側の進入を防ぐ。ただし快速側の信号を停止に変えただけでは普通列車へ許可できない。既に許可を得た快速が接近・通過中ならその進路を維持する。
- 普通線と快速線が独立して合流・交差しないなら、普通列車の発車のために快速側を止める必要はない。
- 検知できる位置・区間占有・進入許可に基づいて判断し、プレイヤーの将来操作や未検知の無許可進入まで予測しない。
- ATOは必ず信号現示・進入許可・有効な安全制約に従う。ただし他列車の突然の無許可進入などに対する無事故まで保証しない。
- 手動で安全装置ONなら各装置の担当範囲で警告・制御する。OFFなら警告・情報表示のみとし、信号無視を防ぐ強制停止を別途追加しない。
- 無許可進入を検知したら占有へ反映し、新たな競合許可を止める。既に許可を得て走行する列車との衝突まで必ず回避できるとはしない。
- 信号無視や、検知時点で停止距離が不足する事象による事故は起こり得るものとする。一方、競合する許可の二重発行やATO自身の信号無視は容認対象ではなく不具合とする。

## 10. 手動の行先指定と進路選択

### 10.1 任意の行先指定（決定）

- マスコンで次の行先を指定できる。主に次の停車予定駅までの距離・駅情報の案内に使用する。終着駅だけを意味しない。必須ではなく、未指定でも手動運転可能。
- 指定行先に適する経路だけへ、選択可能な分岐先や対象区間を限定しない。
- 行先指定だけで実際に通る分岐が確定したとは扱わない。
- 指定駅へ到達できると確認できた進路候補には「行先方面」の目印を付ける。他の候補も表示・選択可能とする。
- 行先に合う候補が1つでも、手動運転では行先指定だけで自動選択・自動申請しない。
- 経路が確認できない場合は「経路不明」とし、「到達不能」と区別する。指定駅を黙って別の駅へ切り替えない。
- 駅情報の複数表示と未指定時の最短駅の判定は第11.6節による。

### 10.2 未確定分岐への接近（決定）

- 分岐手前の所定位置までに警戒速度以下へ減速する基準を設け、進路を確認・選択する。
- 他に厳しい有効な制限があれば最小値を採用する。警戒速度未満から警戒速度へ加速させる意味ではない。
- HUDで選択先を受け付け、占有・予約・停止位置・停止可能性を確認して進入可否を判断する。
- 未選択、または選択先へ進入できなければ、分岐手前で停止する基準とする。
- 手動での強制減速・停止は有効な安全装置が担当する。安全装置OFFなら基準・状況を表示し、運転判断は運転者に委ねる。
- HUDでは「選択済み」と「進入許可済み」を区別する。

### 10.3 確定進路の変更（決定）

- 分岐進入前は変更要求を受け付け、変更先の占有・予約・停止可能性をその都度確認する。
- 安全に切替可能な場合だけ新進路を確定する。
- 変更不可なら元の進路を維持し、理由を表示する。
- 列車がその分岐へ進入した後は、その分岐の選択を変更不可とする。
- 新進路を確保するまでは旧進路を保持する。

### 10.4 通過中の保護と複数進路の保持（決定）

- 先頭が対象へ進入した後は、最後尾が競合範囲を抜けるまで進路を固定・予約し、競合する新規許可を出さない。単一の分岐点を後尾が通過しただけで必ず解放する意味ではない。
- 安全装置OFFでも進路固定・競合管理は維持する。ただし進路管理から速度を強制制御するものではない。
- 次の独立した分岐への進路選択・許可申請は、前の進路の後尾通過を待たずに行える。許可は対象進路の安全確認ができた場合だけ出す。
- 1列車が通過中の進路と今後進入する進路を同時に持てる。取消・変更・期限切れは対象ごとに処理し、別の通過中進路の保護を解除しない。
- HUDは今後進入する分岐の選択・許可状況を主に表示し、通過中の進路は補助表示する。

### 10.5 選択と申請の維持・有効範囲（決定）

- 手動で選んだ希望進路が占有等で使えない間も選択・申請を保持し、確保可能になれば改めて選び直すことなく許可する。別の空いた進路へ勝手に変更しない。ATOの駅名条件に対する候補再選択は第10.13節の別規則。
- 手動運転では許可取得だけでPノッチへ変更・自動発進しない。
- 行先は分岐通過後も維持する。進路選択は対象の分岐、または一体として扱う連続分岐群に対して有効とする。
- 最後尾が対象の競合範囲を抜けたら、その進路選択と使用済み許可を終了する。次の独立した分岐では改めて選択する。前回の左右選択を自動適用しない。
- 例：「ホーム1から上り本線へ出る」途中に複数分岐があっても、一体の進路として1回で選べるようにする。範囲の定義方針は第10.11節による。

### 10.6 複数列車の許可裁定（決定）

1. 現在占有・通過中の列車の進路を後尾通過まで保護する。
2. 既に出した許可を維持する。後から来た高等級列車のために取り上げない。線路変更等の異常や、自発的な変更・取消は別の規則による。
3. 未許可の申請のうち、現在実際に確保できる進路だけを優先度比較の対象とする。使えない進路を待つ高等級列車だけを理由に、確保できる低等級列車の進路を塞がない。
4. 通常は高等級を優先し、同等級なら先に待ち始めた列車を優先する。
5. 長時間待機には等級を超える救済を設け、低等級が無期限に待ち続けないようにする。プロファイルの等級自体を書き換えるものではない。

- 待機救済の閾値・計算方法は全体整理後に決める。将来の空きを作るため新たな競合許可を一時保留するか等、具体的な救済方法は未決定。

### 10.7 停止中の未使用許可の期限と再要求（決定）

- 手動列車が進入前に停止したまま許可を一定時間使用しない場合、HUDで予告し、その後も未使用なら許可を解除する。
- 進入に向けて走行・接近中、または進入済みの列車は、この期限による解除対象にしない。
- 選択済みの進路は残す。ただし期限切れ直後の自動再申請は停止し、運転手がHUDの「進入許可を再要求」を押してから許可待ちへ戻る。
- 通常の占有待ちでは第10.5節の自動取得を維持する。期限切れの再要求待ちと混同しない。
- 高等級による許可の横取りとは別の仕組みである。予告・期限の具体値、再要求後の待機時間の扱い、ATOでの未使用許可の扱いは未決定。

### 10.8 手動での進路選択取消（決定）

| 状態 | 取消の扱い |
| --- | --- |
| 許可待ち | HUDから選択と申請を取り消し、進路未選択へ戻せる |
| 許可取得後・進入前・停止中 | 選択と許可を解除できる。ほかの列車が確保可能になる |
| 許可取得後・進入に向けて走行中 | 許可だけを即時取消する操作は拒否し、先に停止するよう案内する |
| 進入後 | 取消不可。最後尾が競合範囲を抜けるまで維持する |

- 取消操作自体ではノッチを変更しない。安全確認を経た別進路への切替（第10.3節）とは区別する。

### 10.9 線路・分岐の変更時（決定）

- 選択進路が使えるままなら接続を再確認して選択を維持する。ただし変更の影響を受けた許可は再確認まで有効と扱わない。
- 選択進路がなくなったら選択と許可を無効にし、HUDで選び直しを案内する。別進路へ自動変更しない。
- 分岐先の接続が残り、その先が不通となった場合は、残存する進路と不通・線路変更の状況を表示し、表示順位を下げる。表示に残すことは旧経路の選択や許可を有効に保つことではない。
- 分岐の線路が根元から完全に撤去され、進む接続自体が消えた場合は、自動抽出・表示の対象から外す。過去に存在したという理由だけで残さない。数値上の長さ0ではなく、実際の接続の有無で判断する。
- 例：下方から進む3分岐で、右分岐の線路が残って終端になっていれば「右分岐進路 — 行き止まり」と表示する。右分岐が完全になくなれば左・直進の2進路だけを表示する。情報不足で確認できないだけの場合は消失と断定しない。
- この表示順位は列車の等級・許可裁定の優先順位とは別とする。不通と、一時的な停止現示・占有待ち、情報不足による未確認を区別する。
- 残存進路に付随する変更履歴・不通情報の保持期間、再起動をまたぐ保存方法は未決定。ただし消失した分岐を一覧へ残すための恒久登録は、現段階では前提にしない。設置者による明示的削除は第10.11節の将来検討へ送る。
- 走行中に影響を受けた場合、安全装置ONなら担当機能が停止に向けて制御し、OFFの手動運転なら警告のみとする。
- 進入済み範囲は線路変更だけを理由に空き扱いにしない。列車位置・競合範囲が確認できるまで、新たな競合許可を保留する。
- 線路消失による脱線・走行不能等の挙動や復旧手順は別途決める。

### 10.10 共通の進路管理（決定）

- サーバー側の共通機能が許可判断を担い、マスコン・ATOは希望進路を申請する。情報・要求・結果の車上共有はINTEROS経由とする。
- 信号は進路状態と先の区間条件に基づいて現示を決定し、HUDは申請結果・待機理由を表示する。
- 共通とは許可責任・ルールの一本化を意味し、全路線を毎回一括走査する意味ではない。将来は区間・エリアごとに処理を分割できる構造とする。
- 共通の進路管理とATOSの役割を分ける。ATOSが存在しない路線という区分は設けず、ATOSも常時稼働するが、許可裁定をATOSの自律的な運行操作とは扱わない。

### 10.11 進路情報の範囲と定義方法

**現段階の範囲（決定）：**

- 自動抽出した進路情報のみを使用する。手動登録や、設置者による編集・削除を前提とした管理機能は初期実装へ含めない。
- サーバー側の共通の進路管理で扱い、HUDは参照・選択の入口とする。専用の編集画面・設備や恒久的な登録簿の形式を決めたものではない。
- 接続が存在する進路は、信号の有無や先の線路の状態だけを理由に除外しない。進路、その先の状況、進入許可を別々に扱い、信号なし・終端・停止・占有・不通・未確認等を明示する。接続自体が消失した場合は第10.9節による。
- 既知の進路が不通になった場合は第10.9節による。存在するか分からない未探索の経路を推測で作成する意味ではない。
- 到達駅や出口信号が同じでも、途中の経路が違えば別の進路候補とする。HUDで経由先を区別し、経路別の距離・占有・許可を扱う。

**抽出方式（変更可能な暫定方針）：**

- 線路の接続から候補を抽出する。信号等の進入前に待機する境界から次の境界へ至る連続分岐群を1候補とする方向だが、信号のない進路も扱うため、その場合の区切り方は別途決める。
- 経由先・方向が分かる表示を目指す。現段階の自動表示名の生成規則は未決定であり、任意の名前変更を要求しない。
- 候補抽出と許可・競合判定を分け、後から方式を変えられる構造とする。具体API・データ形式・実現可能性の確認は未完了。

**将来の機能追加時に検討（詳細・採否は未確定）：**

- 不要になった進路情報の明示的な削除。
- 進路の名前変更、設置者によるまとまりの補正などの編集機能。その画面・設備、権限、保存・復旧方法は同じ段階で検討する。
- 以前の「基本は自動抽出し、設置者が補正する」という案の補正部分は、現段階の必須実装としない。
- 将来の手動管理情報の保持・削除方式は未確定。現段階の自動抽出では、完全に消失した分岐を表示から外すために手動削除機能を必要としない。

### 要確認

- 警戒速度へ到達すべき位置、確認位置、停止位置、進入判定点、先頭・最後尾の基準、競合範囲の幾何的な定義。
- キーでの候補選択・確定の詳細、マウス選択との関係、複数進路の表示件数・切替方法。
- 再構築された線路と旧進路・列車位置の照合、変更拒否時に旧進路も無効な場合の具体的な復旧、逆走・後退・分割連結。
- 許可・占有・進路識別子の正本と永続化、標準Create列車との共存、同一tick内の処理順、切断・再起動・情報欠損時の扱い。
- 手動行先情報とCreate Navigationの目的地をどのように分離するか。

### 10.12 駅名指定・サジェスト・入力検証（決定）

- 駅指定はCreateに近い名前ベースに統一する。サジェストは駅名入力補助であり、選んだ特定駅のIDへ固定しない。個別ID指定モードと名前指定モードを分ける以前の提案は採用しない。
- 1つの入力欄でサジェスト・直接入力・`*`を含む条件を扱う。`*`なしは完全一致名、ありはパターン一致。同名駅はサジェストでどれを選んでも候補になる。一致候補をGUIで確認できる。
- 改名には自動追従しない。旧BをCへ改名し新Bを作った場合、A→Bの時刻表はAと新Bに停車し、Cに停車させるには時刻表へ追加する。
- 運行スケジュールは現在存在しない駅名も入力・保存でき、後から駅を作る運用を許容する。運行時に利用できなければエラー停止する。入力中の事前案内は候補であり必須ではない。
- 変更予約等、現在存在する必要がある指定は一致駅なしを拒否する。ワイルドカードでも少なくとも1駅必要。さらに現在の時刻表に将来の該当停車が見込めるかを確認する。切替先時刻表までは考慮しない。
- 入力文字列を保持することと、具体的な駅・経路を選んで許可・承認へ結び付けることは別。内部で駅IDを使うこと自体は禁止しない。
- 空文字・不正な文字列・長さ・エスケープ・ワイルドカードの全構文は未決定。存在チェックの緩和から無制限な不正入力の許容を推測しない。

### 10.13 ATOの空き候補駅選択（決定）

- 名前条件に一致する到達可能な候補から、空いていて進路を確保できる駅を優先し、その中で現在位置から線路沿いの経路距離が近い順に選ぶ。直線距離ではない。同距離の裁定は未決定。
- 候補がすべて使用中・進路確保待ちならエラーではなく空き／進路待ち。通常は分岐進入元の停止信号手前で停車する。信号なしでも未許可で進入せず分岐手前で待つが、具体的停止基準は未決定。
- 待機中は空き・進路状況を再評価し、進入前で安全に変更できれば別候補へ申請できる。許可・信号が整えば追加操作なしでATO再開。選択済み・進入済みの保護を壊してまで最短へ切り替えない。
- 複数列車が同じ駅を選んでも共通進路管理が許可を裁定する。既存の等級・待機順・救済を使い、既許可を横取りしない。不許可側は別候補を再評価し、なければ待機する。
- 手動行先指定だけでの自動進路選択は行わない。自動選択と、変更予約の名前条件に一致した停車で適用することも別。
- 未選択の候補駅が失われただけなら残候補から選ぶ。選択済み目標が失われた場合はエラー停止し、別候補でも具体駅・経路への再発車承認を必要とする。
- 情報不足は消失と分け第7.17節による。予定外の後退が必要なら手動退避。承認待ちの具体対象が変われば旧承認を再利用しない。

### 10.14 競合する無許可侵入の検知後（決定、検知性能は未検証）

- 許可進路または競合範囲への他列車の侵入を検知したら停止を優先する。無関係の独立線路の列車まで停止原因としない。制動余裕に応じた常用／EB判定は数理設計が必要。
- 停止後に現在位置から目的地への経路を再評価する。分岐進入前で安全に変更できれば代替へ申請し、代替なしなら待機する。
- 進入・占有済み部分は保護を維持し、再検索だけで空き扱い・切替をしない。新経路の許可を得て、安全に解放できる旧部分だけを解放する。
- 再開前に現在の占有・他列車の許可・旧許可の有効性を共通管理で再確認する。プレイヤーの追加承認ではない。
- EBなしの競合停止なら、競合解消・経路・許可・信号の成立後にATO自動再開。EBや目標喪失エラーが併発したら各復旧条件を適用する。
- 手動で安全装置OFFなら警告・表示のみ。検知遅延・制動距離不足による事故まで防ぐ保証はしない。Create占有情報は材料だが検知範囲・タイミング・CAT実装は未検証。

## 11. マスコンHUDと入力

### 11.1 画面構成（決定）

- 手動の運転操作にはマスコンHUDを開くことを必須とする。マスコンをクリックして表示する。
- 画面の左・右・下をHUDとし、中央から上に前方のゲーム内景色が見える領域を確保する。
- 参照画像の斜線は景色。ホットバー非表示固定の初期案は変更し、表示／非表示をプレイヤー設定で選ぶ方向。既定値は未決定。E等でインベントリを開いたらマスコンHUDは閉じ、第11.4節の処理を行う。
- 参照画像：利用者提示の `C:/Users/mii-r/Downloads/タイトルなし.png`。リポジトリには今回コピーしていない。画像がなくても上記配置方針は本書から読める。
- ノッチ・進路のキー操作に加え、ATO有効ボタンなどのHUDボタンをマウスで操作できる。

### 11.2 視点とカーソル（決定）

- 通常はマウスで視点を動かす。専用キーを押している間だけHUD用カーソルを操作する方式を初期設定とする。
- プレイヤー設定で「押すたびに切替」も選べる。
- HUDマウス操作中は視点を固定する。クリックを背後のブロック操作や攻撃へ伝えない。
- 視点操作へ復帰するとき、カーソル移動量で視点が飛ばないようにする。
- HUDボタン操作中もノッチキーは受け付ける。文字入力中は誤操作防止のため別途規則を決める。
- 実際のキー割当、画面内の詳細配置、表示倍率などは未決定。

### 11.3 レバー（決定）

- HUD上に実物のマスコンを模したレバーを設け、ノッチ位置のクリックまたはドラッグで変更する。
- プロファイルに定義された離散ノッチへ切り替える。キー操作もレバー表示へ反映する。
- マウスとキーで同じノッチ要求処理を使用する。ATO中なら手動切替を起こし、加減速度移行則に従う。
- EBへの追加操作とEB中のグレーアウトは第6章による。
- ドラッグ途中の各段の確定時点、異なる入力方式の同時操作・引継ぎは未決定。

### 11.4 手動運転中にHUDを閉じる（決定）

| 状態・操作 | 動作 |
| --- | --- |
| 操縦担当・ATO OFF・走行中に通常終了 | プレイヤー別の終了ノッチを適用。Nまたは通常Bから選択、既定は最大常用。Pは選べない |
| E等でインベントリを開く | HUDを閉じ、通常終了と同じ処理 |
| 手動操縦担当がHUDを開いたまま切断 | 終了ノッチ設定にかかわらず最大常用ブレーキ。瞬間停止ではない |
| 正常にHUDを閉じた後で切断 | 切断による追加制御を行わず、終了時に適用した扱いを維持 |
| ATO ONで通常終了 | HUD終了だけではATOを解除しない。使用台に必要なキーがあれば継続 |
| 閲覧者・操縦開始前のHUD終了 | ノッチやATOを変更しない |
| HUDを再び開く | 開いただけでは終了処理の制動を解除せず、操縦担当取得後の明示操作を必要とする |

- HUD終了やATO OFFだけでATACSをOFFにしない。有効な安全装置は作動を続け、終了ノッチより優先する。EBも継続する。
- 最新追加：手動走行中のHUD終了による最大常用ブレーキ時には、GUIの指定ノッチをNへ自動変更し、内部指定値もNにする。切替時点とブレーキ保持の接続は第11.10節の未決定事項。単にN要求だけを出して制動を失わせる実装にしない。
- 「閉じると常にNへ移行して惰行する」という旧終了表は廃止。旧非N終了警告を新しい終了ノッチ設定にどう適合させるかは要確認。採用する確認画面のEscは第11.5節に従う。
- ATO ONで担当者がHUDを開いたまま切断した場合、死亡・マスコン破壊・終了競合、停止中の終了・勾配保持の全規則は未決定。手動時の切断制動を無人ATOへ無断で一般化しない。

### 11.5 Escの統一（決定）

- 警告・確認・選択画面ではEscを「戻る／キャンセル」に統一する。承認・実行として扱わない。
- 一度のEscは最前面の画面だけを閉じ、背後のマスコンHUDまで連続して閉じない。
- HUD終了警告でEsc：終了をキャンセルし、現在のノッチを維持して運転へ戻る。
- ATO解除後の安全装置選択でEsc：選択画面を閉じ、安全装置ONのまま手動を続ける。
- EB解除不可等のエラーでEsc：表示だけ閉じ、EBや解除条件は変えない。
- その他の確認画面でEsc：未確定操作をキャンセルして直前の画面へ戻る。

### 11.6 複数の駅情報と主表示（決定）

- HUDは駅情報を1駅に限定せず、次の次の駅、最短経路上の駅、分岐先の候補駅等の複数情報を表示する。
- 行先指定あり：次の停車予定駅を主表示とし、その駅までの距離・駅情報を案内する。例として「現在地→A→B」でB停車予定なら、Aの方が近くても主表示をBにする。途中駅・その先の駅の併記は妨げない。
- 行先指定なし：進行方向に線路沿いで到達できる駅のうち最短の駅を主表示にし、その先や別候補の駅も表示する。直線距離では判定せず、背後の駅を単に近いという理由で採用しない。
- 選択済み進路があればその経路に沿って判定する。未選択の分岐があれば候補経路中の最短駅を暫定表示し、「進路未確定」と分かる表示にする。
- 分岐先の駅にはどの経路上かを示す。行先指定は表示対象を1駅に限定したり、通れる進路を限定したりする機能ではない。
- 指定駅の経路を確認できなくなっても別の駅へ黙って主表示を変更せず、「経路不明」等を示す。
- 表示件数、並び順、展開方法、探索上限、同名駅の識別、指定駅通過・到着後の更新は未決定。

### 11.7 次の信号現示と距離（決定）

- 現在の進行方向で次の区間へ入る際に従う信号の現示と、その信号までの線路沿いの距離を、マスコンHUDに常時表示する。
- 地上信号が描画距離外でも確認できるようにする。描画された色を読み取るのではなく、サーバー側で管理する信号情報をINTEROS経由で表示する。
- 信号通過後は、その次の信号へ対象を切り替える。接近中も現示変更があり得るため、表示によって急制動を完全に防げるという保証ではない。
- 情報を取得できないときは「不明」とし、古い進行現示を最新情報のように表示し続けない。
- 分岐がある場合、候補経路ごとに最初に出会う信号の現示・距離を表示する。未選択なら各候補を区別し、選択済みなら選択中の経路に対応する表示を明示する。
- HUD案内用の取得・表示は各候補経路の次の1基まで。その信号より先の「次の次」まで追わない。これはHUD案内の範囲であり、駅情報の複数表示とは別の要件である。
- 例：「直進側：進行／○○m」「左分岐側：停止／○○m」。2つは連続する信号ではなく、別経路の次の信号である。他経路の進行現示を自分の許可と取り違えない表示にする。
- 利用者の線路図は、下から上へ進み、分岐前の共通線に1基、その先の直進側と左分岐側に各1基の信号がある配置。分岐前の信号がまだ前方なら、それだけが次の信号になる。通過後は直進側・左分岐側の各1基が次の信号候補になる。
- 参照画像：`C:/Users/mii-r/AppData/Local/Temp/codex-clipboard-46c7f789-4ed6-41f1-bb2e-225c430bdd81.png`。横棒が信号位置を示す。画像はリポジトリへコピーしておらず、一時ファイルが消えても前項の文章で配置を再現できる。
- 信号が見つからない場合は、終端まで確認して信号がなかった状態、探索上限に達して先を確認していない状態、情報欠損・更新中等で確認できない状態を区別する。表示案はそれぞれ「次信号なし／線路終端あり」「次信号未確認／探索範囲外」「次信号不明／情報取得不可」。
- 信号が見つからないことを進行現示や進入許可の代わりにしない。循環検出と探索量の上限を設けるが、同じ出口へ至る別経路を循環と誤認して消さない。
- 更新頻度、情報の有効期限、方向別信号の対応付け、探索上限の具体値・方式、未読込時の具体処理、地上描画との同期は未決定。制動計算等の内部探索範囲までこのHUD要件だけで決定しない。

### 11.8 運転情報表示・実効ノッチ（決定）

- 指定ノッチ（内部の指定レバー位置）と、実際に適用されている加減速度から逆算した実効ノッチを別々に表示する。
- 実効ノッチは常に整数。現在の走行条件で最も近いノッチ性能へ換算し、境界で頻繁に往復しない表示上の余裕を設ける。小数ノッチ表示は不採用。ATO／TASCの内部連続制御を離散化する意味ではない。
- 実効ノッチの表示更新は既定20tickに1回、プレイヤー別設定で変更可能。制御周期・指定ノッチ・速度・加速度の表示周期まで20tickに制限しない。
- EB中は実効ノッチ欄をEBとし、状態受信時に通常間隔を待たず切り替える。移行途中の減速度は加速度欄で確認する。EB解除時の表示更新時点は未確定。
- 現在速度（km/h）と現在加速度（m/s²、加速＋／減速－）を表示する。加速度は目標指令ではなく、自然減速を含む実際の速度変化を示す。
- Nによる自然減速だけでB作動と誤表示しないよう、実効ノッチ換算は走行条件と自然減速を考慮する。具体式・同値性能ノッチ・勾配・丸め・表示平滑化は未決定。
- 目標加速度は基本的に内部値に留める。デバッグHUDは将来の構想として別途検討する。
- 運転情報ブロック（仮）を用意し、キー不要の読み取り専用情報表示に使う。マスコン使用中でも他者が閲覧でき、操作権は得ない。情報はINTEROSから取得。表示項目・接続方法・配置・閲覧人数等の詳細は未決定。

### 11.9 共通キー・HUD占有・操縦担当（決定）

**共通キー：**

- 列車登録不要の共通キーを残す。所有者・管理者・列車識別には使わず、電車らしい操作のためのアイテムとする。操作権や通知先をキーの所持者から推測しない。
- Server Configでキー必須ON／OFFを選択し、既定ON。OFFなら手動・ATOとも不要。ONなら使用するマスコンのキー枠に必要。
- HUDにアイテム枠を設け、キーを入れると操作部のグレーアウトを解除できる。専用の挿鍵動作は不要。インベントリ所持だけで判定する案は不採用。
- キーは消費せずマスコン側へ保持し、HUDを閉じても残る。主にキーがあっても、副を使用するなら副に必要。無人ATO継続にも使用台への挿入が必要で、両台へ残せば予定された切替・折返しが可能。
- 必須ONで使用中のキーを取り出したら、手動／ATOとも最大常用ブレーキで停止させる。ATOは解除。終了ノッチ個人設定より優先し、有効な安全制御・EBは維持する。
- 入れ直しただけでは制動解除・ATO再開をしない。手動は改めてノッチ操作、ATOは従来のATOボタンで開始項目等を確認して再開する。別の承認ボタンを追加しない。
- 使用していない台からキーを取り出しただけでは現在のATOを停止させない。ただし別の操縦担当がいる閲覧者には取出し操作自体を認めない。
- キーを残してHUDを閉じた本人へ、取り外し忘れ音を一度鳴らす。プレイヤー別ON／OFF、既定ON。キー必須のServer Configとは独立。自動取出し・終了拒否・鳴り続ける警報ではなく、ATO継続を妨げない。

**HUDと操縦権：**

- 同じマスコンのHUDを同時に開けるのは1人。主と副など別の台のHUDは別々の人が開ける。列車全体のHUD占有にはしない。
- HUDはまず閲覧状態で開く。キーなしでも速度等の計器を監視できる。「操縦開始」ボタンで列車全体の操縦担当を取得する。
- 条件を満たして先に操縦開始した1人を優先する。先にHUDを開いただけでは優先権を得ず、主副にも優先差はない。同時要求の具体的な処理順は実装設計事項。
- 誰も操縦していない間は、操縦開始の準備としてキー挿入を許す。それ以外の列車状態変更は担当取得後に行う。必須OFFなら挿入不要。
- 担当取得だけではノッチやATOを変更しない。担当者のノッチ操作は既定の手動介入となる。閲覧中はマウス・キーの双方で運転入力を受け付けない。
- 他の操縦担当がいる場合は、閲覧者の操縦開始、キー／時刻表取出し、安全装置変更等の列車状態変更を無効化する。閲覧と本人の表示設定は可能。マスコンを開いたままでも状態に応じ制限を更新する。
- 担当がHUDを閉じる・切断する場合は該当する終了制御を行って担当を解除する。別の閲覧者へ自動移譲しない。次の人は明示的に操縦開始する。閲覧者の終了はノッチ・ATOに影響しない。
- ATO走行中は使用中の台からだけ担当取得可能。別の台は閲覧のみで、停止後に使用台を明示切替して取得できる。走行中の使用台切替はしない。
- 停止中の使用台切替だけでは列車方向を変えず、新しい台を基準に前後表示を合わせる。中立時の引継ぎや手動走行中の全切替ケースは第11.10節と合わせて設計する。
- 連結・分離で複数担当となる場合、キー喪失後の担当保持、遠隔操作との競合、操作可能距離・降車・死亡などは未決定。

### 11.10 レバーサーと自動レバー表示（2026-09-22の最新方針）

**決定したこと：**

- レバーサーは前進・中立（ニュートラル）・後進の3位置。中立ではノッチ操作等を行っても何も起きないという操作無効の方針。ノッチN（自然減速）とは異なる。
- 前後方向へ動かすレバーとする。HUD上のノッチ・レバーサーの切替アニメーションは滑らかにし、位置が突然飛ぶような不自然な表現にしない。
- ノッチ・レバーサーの自動切替を、内部だけでなくHUD上のレバー位置の変化として表現してよい。表示だけの見せかけではなく、対応する内部指定値も変更されたことを示す。
- 手動走行中にHUDを閉じ、最大常用ブレーキが適用される場面では、指定ノッチを自動でNへ変更する。これはノッチ内部値の変更も伴う。切替タイミングは未定。
- ノッチ自動切替には制限を設ける。ATO動作時等の除外を含め、全場面を一律に自動追従させる仕様ではない。実効ノッチ表示に合わせて指定レバーを常に動かす決定でもない。

**調査中・次に整理すること：**

1. 手動レバーサーを走行中に操作できるか、操作した場合の動作は利用者が調査中。「完全停止かつNのみ受付」という直前の提案は未承認であり確定条件にしない。スケジュールの方向設定を停止中に実行する既決定とは別。
2. 中立で無効になる範囲：P・通常B・手動EB・ATO開始・各ボタン、ノッチ位置自体を動かせるか、既存の入力を保持するか、復帰時に古いP等を再実行するか。ユーザーの「何をされても何も起きない」を、安全装置・EB・終了時制動まで停止する意味へ自動拡張しない。
3. 中立へ移した際の実際の加減速度・保持制動・設定方向、ATACS／EBとの優先関係。基本安全制御を維持する従来方針との整合を利用者と確認する。
4. HUD終了で最大常用制動を要求する時点、指定ノッチNへの変更時点、停止後／再表示後の制動保持・解除。指定Nへ変えることと最大常用制動の継続を両立させる状態設計が必要で、Nへの変更を制動解除とみなさない。
5. 終了設定が最大以外のBまたはNのとき、切断・キー取出し・時刻表取出し・TASC停車完了・EB復旧・主副切替のどこで自動レバー変更を使うか。今回の限定例を全ケースへ拡張しない。
6. ATO中に指定レバーを動かす／動かさない条件、レバー自動移動を手動入力と誤認してATO解除しないための区別。アニメーションと実制御の発効時点・同期、ドラッグとの競合、通信遅延・再表示時の描画。
7. アニメーション時間・補間方法は未決定。物理的な加減速度の10tick線形移行や実効ノッチ20tick更新と、GUIの動きを自動的に同一設定へ結び付けない。

## 12. 調査・実機報告の根拠

調査対象は `vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/`。以下のパスはリポジトリ内の実ソースを指す。行番号は今回調査時点の目安であり、ソース更新時は再確認する。

### 12.1 区間・経路・表示・更新順の事実

| 調査事実 | 根拠 |
| --- | --- |
| 信号区間は列車占有・予約・交差関係を持ち、交差先を含めた占有を判定する | [SignalEdgeGroup.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/SignalEdgeGroup.java) 21行付近以降 |
| 先頭側で区間を占有し、後尾側で解放する処理がある | [Train.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/entity/Train.java) 457・505行付近 |
| Navigationは目的地なしでtickを終了し、手動時に信号停止判定を迂回する箇所がある | [Navigation.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/entity/Navigation.java) 73・164・303行付近 |
| 目的地ありでは予定経路、なしでは手動操舵を用いる経路選択がある | 同Navigation 347行付近 |
| 信号先読みは経路をたどって境界と距離を受け取る。ただし標準の速度制御・予約も同じ処理に含まれる | 同Navigation 126行付近以降 |
| 毎tick、占有集合と予約をクリアし、グラフ処理・列車earlyTick/tick・信号更新へ進む | [GlobalRailwayManager.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/GlobalRailwayManager.java) 189・219行付近 |
| Createの連鎖信号の黄は経路先の信号状態の集約結果であり、CATの「注意」の速度値ではない | [SignalBoundary.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/SignalBoundary.java) 223行付近 |
| 信号ブロックには赤への表示切替を遅らせる処理がある | [SignalBlockEntity.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/SignalBlockEntity.java) 155行付近 |
| 信号区間再構築では新しいUUIDを生成する | [SignalPropagator.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/SignalPropagator.java) 75行付近 |

これらからの設計上の推論：表示色だけを安全判断の正本にしない。CAT用の手動先読み、予約の責任主体、同一tick内の参照・反映順序、経路再構築時のキャッシュ破棄が必要。実装方法と性能は未検証。

### 12.2 速度処理の事実

- Navigationは `v²/(2a)` を使う標準の停止距離計算、接近時の直接速度補正、目標速度代入と `approachTargetSpeed(1)` を行う（同ファイル100・235～289行付近）。CATの移行時間付き制動仕様をそのまま満たすものではない。
- Trainの信号・駅到達処理には `speed = 0` がある（444・461行付近）。
- Trainには通常の自然減速がある（568行付近）。
- ブロック・脱線・衝突等にも速度変更がある。通常の制御置換と障害物・事故処理の責任は分けて調べる。

### 12.3 曲線距離の追加調査

**直接確認した事実：**

- [BezierConnection.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/track/BezierConnection.java) 341～400行付近：曲線全長は16分割の点間距離和による近似。別にstepLUTを持つ。
- 同206行付近：小刻みな移動のパラメーター更新は現在位置の微分の大きさを使う。
- [TravellingPoint.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/entity/TravellingPoint.java) 199行付近と401行付近：`t = position / edge.getLength()` を用い、更新後に `position = t * length` とする。曲線上のpositionは常に実弧長そのものではない。
- 同214行付近：長距離探索の不正確さと回避処理に関するコメントが残る。
- [TrackEdge.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/graph/TrackEdge.java) 81～93行付近：長さとincrementTの処理。移動距離絶対値が5を超える場合は曲線の微分補正を使わない分岐がある。
- Navigation 141行付近：先読み距離へ50を追加する回避処理がある。精度保証ではない。
- TravellingPoint 357行付近：信号への距離通知は辺の長さと辺上の位置から組み立てられる。

**導出・説明用の数値計算（Minecraft実測ではない）：**

半径16相当の90度曲線を表す3次ベジェ、制御点距離 `16 × 4/3 × tan(π/8)` を使い、倍精度で比較した。

| 値 | 計算結果の概数 |
| --- | --- |
| 16分割の折線近似全長 | 25.126159 |
| 高分割Simpson積分の全長 | 25.136267 |
| t=0.25までを近似全長×0.25で求める | 6.281540 |
| t=0.25までを曲線に沿って積分する | 6.396314 |

全長近似の差と、途中のパラメーター位置を距離とみなす差は別である。この例は最大誤差・CATの許容値・実ワールド上の誤差を決めるものではない。倍精度計算であり、Minecraftのfloat演算を完全再現したものでもない。

**改善案（未実装・未承認の具体方式）：**

- 曲線パラメーターと累積弧長の対応表をCAT側で持ち、位置を距離へ換算する。
- 経路上の完全な辺と、端の部分辺をそれぞれ積算する。単純なposition差へ依存しない。
- 曲線ごとの表を再利用し、線路変更時は破棄・再構築する。
- 実際のCreateの小刻みな移動と幾何距離は別に比較する。距離を補正しただけで停止精度まで保証済みとはしない。

未確認：直線・曲線途中・曲線境界・逆方向・勾配・分岐・次元境界での誤差上限、計算負荷、読込境界での情報可用性、停止予測との整合。

### 12.4 進路候補と次信号表示の追加調査

ローカルソースから確認した事実：

- [SignalPropagator.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/SignalPropagator.java) 112行付近：`collectChainedSignals`は分岐先の信号を集め、信号IDと向きの対応を返す。返り値に経由分岐一覧・距離は含まれず、同じ信号に至る別経路をそれぞれ表す形式ではない。
- [EdgeData.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/graph/EdgeData.java) の`getPoints`・`next`、TrackGraphの`getConnectionsFrom`、TrackEdgeの`canTravelTo`に、線路上の情報と進行可能な接続を調べる材料がある。
- [SignalBoundary.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/SignalBoundary.java) は方向別の`cachedStates`を保持する。一方、322行付近の`write(FriendlyByteBuf, ...)`では基底情報と区間IDを書き込み、現示は含めていない。確認したこの通信経路だけで描画距離外の最新現示をHUDへ送れるとはいえない。

設計上の推論：CATは候補経路と信号・距離の対応を保持し、HUDへ必要な情報を届ける方式を設計する必要がある。材料の存在は確認したが、完成機能としてそのまま流用できること、精度・負荷・更新整合性は未検証。実装・ビルド・ゲーム内試験は行っていない。

### 12.5 実車TASCの参考調査（CAT仕様とは区別）

- [JR東日本・2008年のホーム柵導入資料（別紙2）](https://www.jreast.co.jp/press/2008/20080603.pdf) は、停止位置までの距離からTASCパターンを生成し、接近に応じてブレーキを調整する仕組みを説明している。
- [JR東日本公式解説（2024年12月24日更新）](https://media.jreast.co.jp/articles/35) は、山手線の改良型TASCを手動ブレーキ主体の補助として説明している。同記事のB1等の操作案内はシミュレータ向けであり、全路線・車両共通の実車取扱いとして一般化しない。
- [鉄道総研・ブレーキ力低下の補償研究](https://www.rtri.or.jp/rd/seika/2020/1-07.html) は、同じ指令でも減速効果が変動し、運転士やTASCが指令を調整して停止精度を確保することを説明している。
- 第7.5～7.11節の可変上下限、通常性能範囲内の力行補助、ボタン自動停止、後退修正はCATの承認仕様であり、これらの資料から実車の同一動作を確認したという意味ではない。実車の距離・速度・精度値をそのままCATの設定値へ採用しない。

### 12.6 運転台方向・編成変更の別チャット調査

根拠：[Create 6.0.8 運転台方向・列車進行方向・編成変更の調査](調査関連/CREATE_6_0_8_CAB_DIRECTION_AND_CONSIST_CHANGES.md)（2026-09-21）。以下は同報告からの要点であり、CAT実装済みの証明ではない。

- 組立ブロックのローカル位置・BlockState、車両の組立方向・順序により、各台の方向と最外側を判定する材料がある。曲線上の現在ワールドyawだけで判定しない。
- 標準の前後運転台フラグは車両内の存在情報であり、個体ID・主副・最外側を表さない。標準スケジュールにも使用台個体の指定はない。
- CAT専用台では標準運転台の走査が自動適用されない。標準組立の前向き運転台条件と、NavigationのConductor／doubleEnded条件へ対応する設計が必要。
- 車両インデックスは不変IDではなく、Carriage.idは永続保存されない。再組立ではTrain UUIDも変わる。CAT独自の永続マスコンIDは候補であり、保存先・複製対策・継承は未確定。
- 生Entityがない未読込状態もある。他アドオンの車両順序・反転・NBT・UUIDの変換と完了通知は別ソースが必要。
- 報告作成時は連結アドオン未指定だったが、その後の利用者報告でSteam ’n’ Railsと判明した。対象版と内部判定の確認は未完了。第2.3節の実機報告から内部実装を断定しない。

### 12.7 駅名・ワイルドカード・経路評価の会話内ソース確認

- [DestinationInstruction.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/schedule/destination/DestinationInstruction.java) は駅名条件を正規表現へ変換し、同じグラフの一致駅群をNavigationへ渡す。入力GUIの`*`数制限等をCATの仕様へ自動継承する決定ではない。
- [Navigation.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/entity/Navigation.java) の探索は距離に加え他列車・駅・信号占有等のペナルティを考慮する。「空いている駅だけを厳密に選ぶ」「到着直前まで常に別ホームへ変更する」とは確認していない。CATの空き・許可優先の具体実装は別設計。
- [TrackEdgePoint.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/signal/TrackEdgePoint.java) はUUIDを持ち、[GlobalStation.java](../../vendor/Create-mc1.20.1-6.0.8/src/main/java/com/simibubi/create/content/trains/station/GlobalStation.java) は駅名を別に保存する。標準スケジュールは名前検索なので、内部IDがあっても改名へ自動追従する指定ではない。
- Navigationの選択済み目的地の保存には駅IDを使う。時刻表の名前条件と、具体的な探索結果・承認の対象は分けられる。CATの設計判断は第10.12節による。
- これらはローカルソースの静的確認で、CAT実装・ゲーム内挙動の検証ではない。

## 13. 整理2から残す構想・未処理の項目

以下は今回の会話で完成させていない。元の構想は整理2を参照し、本書で決定したことだけを上書きする。

- 対応環境：Forge 1.20.1・Create 6.0.8を基本とする構想。Steam 'n' Rails、Railways Navigator、Lazy Tick、Better FPSの対象版・合格条件、将来ローダー対応、専用サーバー条件等は未確定。
- 普通列車・高性能・新幹線等の標準プロファイル、基本ノッチP1～P5・N・B1～B7・EBの構想。性能値、追加プロファイルの初期範囲は未確定。
- 時刻表の編集・予約・切替・待機・開始項目・予定折返しの基本は第7・8章で決定。全対応命令一覧、時計時刻の扱い、待機条件の論理構成、保存・同時編集・遠隔権限等は残る。
- TASCの通常補助・自動停止・手動介入・停車完了・EB・後退修正は第7章で決定。停止基準点、開始条件、可変上下限の数式・精度等は未決定。±0.5ブロック程度は整理2の暫定構想で、検証済みの完成要件ではない。
- マスコンキーは登録不要の共通操作用アイテムへ変更済み（第11.9節）。キーで管理者・所有者を識別する旧構想は使用しない。ATOSの形態・遠隔認可・管理権限はキーと分けて未決定として残す。
- 全アイテムをクラフト可能とする構想。レシピ、外観、音、モデルは未確定。
- 保存データ、列車・車両・マスコン等の識別子、同期、分割連結、移行、不正値・欠損・通信断への対応。共通キーへ所有／列車識別子を設ける前提は置かない。
- 性能・安全性・正確性・互換性・保守性の優先順位と合格基準。具体的な数値は全体整理後。
- コメント・命名・構成・ログ等の開発規則、実装順序、試験分担の正式化。
- 特定列車の通過待ち等の高度な時刻表機能は低優先度候補。

### SpeakerControllerの構想を維持

- 各スピーカーをSpeakerControllerへ直接接続する。スピーカー同士の直接・連鎖リンク方式は採用しない。
- 音源をControllerへ設定し、Controllerへのレッドストーン入力、または列車のボタンから再生指令を送る。
- 接続済みスピーカーで同一音源を同期再生する構想。
- 接続距離・最大台数・所属・音源形式／配布・権限・再生競合・未読込・破壊時処理は未決定。旧リンク方式の64ブロックを新方式へ自動継承しない。

### ドキュメント再構築TODOを維持

- AGENTS.mdとドキュメント・仕様書の棚卸し、削除／old保管／維持／再構築の分類、構成・切替順序を決める。
- 原回答の文書構成リンクは現在のファイル名・配置と一致しなかった。本書第1章に実際の下書きへのリンクを置いた。
- この保存で旧文書の削除・移動やAGENTS.mdの書換えは行わない。
- 今後、承認内容を正式文書へ分配する。整理3を完成済みの全仕様と扱わない。

## 14. 整理2からの進捗と残課題の棚卸し

### 14.1 元の矛盾・不足点 C-01～C-12

| ID | 現在の扱い |
| --- | --- |
| C-01 | 互換の意味とCAT速度制御の置換方針は明確化。具体的な介入・互換確認は未完了 |
| C-02 | 併設禁止・組立拒否を決定。検出手段・異常時処理は調査待ち |
| C-03 | ATOの操縦系強制有効、開始前設定保持、解除後の選択設定を決定。全対象一覧は未完了 |
| C-04 | 手動時の安全機能OFFを許容。基本機能は無効化不可。具体的な例外・介入中OFF等は未完了 |
| C-05 | 通常／ATO／TASC／EBの線形移行・設定管理を決定。TASCの通常補助は可変ブレーキ上下限で手動要求を補正する。性能数式・数値・追従性検証は未完了 |
| C-06 | 地上共通現示・車上性能差を採用。区間と距離の併用全体の採否は調査継続 |
| C-07 | 分岐接近・選択・変更・取消・後尾通過までの保護、未使用許可の期限、等級と待機救済の基本方針を決定。候補抽出は暫定、具体的裁定式・競合範囲・実現性は未完了 |
| C-08 | 許可判断はサーバー共通管理、車上の情報・要求はINTEROS経由、ATOSは常時稼働・現段階で自律操作なしと決定。Createとの状態接続・処理順・具体配置は未完了 |
| C-09 | キーは登録不要の共通操作用アイテムへ変更。必須Server Config、キー枠、使用台判定、取出し制動を決定。所有・管理者・遠隔権限の判別に使わない。ATOS認可は別途未決定 |
| C-10 | EBの停止後解除・N操作等に加え、駅オーバーランはTASC、信号はATACS配下ATSと責任分担。TASCのB7超過予測でEB、解除に専用条件は追加しない。検知数式・数値・異常系は未完了 |
| C-11 | 性能等の具体的基準は全体整理後。優先順位・予算は未決定 |
| C-12 | 名称と機能定義の最終化は今後。名称だけから実在技術の仕様を取り込まない |

### 14.2 分野別の現在地

以下の「決定・進展」は仕様検討の進捗であり、機能の実装完了を表さない。整理2の全問題がC-01～C-12だけに収まるわけではないため、周辺項目も含める。

| 分野／本書の参照先 | 整理2から決まった・進んだ点 | これから決める・確認する点 |
| --- | --- | --- |
| Createとの境界（第2・12章） | 標準運転台との併設禁止・組立拒否。CATと重複する速度制御を置換。占有等と速度代入を分離する必要性を調査 | 具体的な接続箇所、標準列車との共存、他アドオンとの値・更新時期の互換性、組立後の異常 |
| 責任分担（第3章） | INTEROSを情報の正本・窓口とし、各制御機能が計算。ATACS一括切替、地上共通管理、ATOS常時稼働・現時点で自律操作なし | 全機能一覧、内部状態・共有状態・認可の担当、配置・通信・故障時の境界 |
| ノッチ・性能（第4・5章） | N惰行、通常10tick基本の線形移行、ATO／TASC連続指令、EB別移行、Server Configでプロファイル共通管理 | 性能モデル、単位・速度依存・勾配等、連続更新時の追従性、各性能値・等級算出式 |
| EB（第6章） | 停止・原因解消後に明示解除。手動はN→解除、ATOS解除はATO由来のみ。早すぎる要求は拒否、EB中はN以外無効 | 複数原因、全発動条件、機能OFF・操作権移動・保存との関係、停止閾値 |
| ATO切替（第7・11章） | 手動介入・安全設定復元・進路引継ぎ・開始項目選択・承認兼用・無人再開・使用台キー確認を決定 | 全入力の切替条件、安全介入中のOFF、切断とATO、キー・レバーサー・表示変更の完全な状態遷移 |
| TASC（第7.5～7.14節） | 対象駅指定、通常補助とボタン自動停止、可変上下限、駅超過のEB、停車完了のN／B条件、後退修正、手前停止・対象変更・共通停止目標 | 具体的な駅・列車基準点、補助開始条件、上下限・停止予測の数式、OFF時出力引継ぎ、ATO停車完了との統合 |
| 目標喪失・再開（第7・10章） | 通常待機と喪失を分離。承認とATO再開を統一し対象変化は再承認。予定外後退は手動 | 承認の有効期間・停止前受付、情報鮮度、モード切替・再読込を含むエラー保持 |
| ATOS・時刻表（第8章） | 停車時予約・一括更新・1件置換・編集による破棄・終着切替コピー・列車名／除外／優先度・競合選択・計時を決定 | 全命令・待機条件、アイテム正本、版・同時編集・遠隔認可、時刻系、切替連鎖 |
| 信号方式（第9・12章） | 地上現示は共通、車上の制動は列車性能別。区間と距離の併用に必要なCreate構造と距離誤差を調査 | 併用方式の最終採否、現示決定表・固定配列の扱い、距離精度・負荷、情報欠損時処理 |
| 進路（第10章） | 既存保護に加え駅名条件・空き候補の近い順選択・待機再評価・無許可侵入検知後の停止再探索を決定 | 競合範囲・進入基準点、裁定式、再評価周期、検知精度、グラフ整合、標準列車との接続 |
| HUD・入力（第11章） | 実効整数ノッチ20tick既定・実加速度・終了ノッチ設定・手動中切断制動・台単位HUD／列車単位操縦権・3位置レバーサー・滑らかな自動表示 | レバーサー走行中操作と中立の作用範囲、終了時Nの時点・制動保持、ATO等での自動表示例外、キー割当・各アニメーション |
| キー・通知（第8.15・11.9節） | 共通キー必須既定ON、主副ごとの枠。通知は組立者＋一部運転手へINTEROSが集約・独立間隔で送信 | 遠隔認可、通知一覧・重大度・時間設定、組立者の連結継承、アイテム満杯・破損 |
| 編成変更・保存（第2・12章） | 固定主副、最外側だけ有効、継続側の時刻表・予約のみ継承、外部操作を予測しない | Steam ’n’ Rails内部調査、永続ID、主副再割当、操作担当・性能・制限の継承、再組立・同期 |
| スピーカー（第13章） | SpeakerControllerから各スピーカーへ直接接続。音源設定とレッドストーン／列車ボタン操作。スピーカー間リンクは不採用 | 距離・台数、音源形式と配布、同期、権限、競合・未読込・破壊時動作 |
| 環境・品質・文書（第13章） | 新規再構築の方針、旧仕様を自動継承しないこと、文書再構築TODOを維持 | 初期リリース範囲、対応版・併用MOD、品質優先順位・性能予算、受入試験、正式AGENTS・文書移行・開発規則 |

### 14.3 今回見えた境界条件と整合確認

以下は新しい決定ではなく、決定済み機能を組み合わせるための残課題である。

1. **通常EB復帰と目標喪失エラー復帰**：前者は追加発車操作なし、後者は再発車承認が必要。両方が重なった状態と解除順序を状態表にする。
2. **承認と対象更新**：制限解除・再発車・時刻表編集で、要求中に対象や版が変わる場合を共通して整理する。具体的な識別形式は未決定。
3. **停止保持とN惰行**：停車完了前のTASC保持、完了後のN、EB保持を混同しない。勾配等での挙動は性能モデルと合わせて検討する。
4. **TASC中断と手動入力**：OFF・対象喪失・手前停止で、残ったP入力や制御出力をどう引き継ぐかを整理する。基本の再開方針だけでは全ケースを覆わない。
5. **同名駅と同じ目標**：名前条件で再探索し、承認は具体駅・経路へ結び付けると決定。改名追従・個別ID指定は不採用。識別と版管理の具体方式は未決定。
6. **情報不明と実際の消失**：情報確認待ちと喪失エラーを区別し、長時間だけで喪失としないと決定。鮮度・検知・復旧の実装条件は残る。
7. **手動での信号無視と検知の限界**：安全装置OFFの手動走行まで強制防止する仕様ではない。通常検知と進路保護の役割を保ち、無事故保証とは区別する。
8. **指定Nと終了時制動**：最新方針ではHUD終了時の最大常用制動と指定Nの自動変更が併存する。指定値・最終制動出力・解除条件を分けて設計し、N表示だけで制動を解除しない。
9. **自動表示と運転入力**：GUIアニメーション・自動指定変更・手動入力・実効ノッチ換算を混同しない。特に自動レバー移動による誤ったATO解除や、アニメーション完了待ちによる安全制動遅延を避ける設計が必要。
10. **中立と安全制御**：レバーサー中立の「操作しても何も起きない」の正確な範囲が必要。ノッチNとは異なり、EB・ATACS等まで無効にする仕様は未承認。

### 14.4 将来対応と未採用案の区別

- 将来対応の方針を決めたもの：サーバー側からの独自プロファイル追加、ATOS制限の区間・エリア単位への拡張を見込んだ構造。
- 将来の検討へ回したもの：進路の明示的な削除・名前変更などの管理機能。初期は自動抽出のみで、管理機能の詳細は未確定。
- 将来候補：ATOSの自律的なダイヤ調整。現時点で自動操作は行わない。
- 将来機能：不要な変更予約の定期整理・一致駅喪失が続く予約の事前通知付き削除。最初期には不要。通常の置換・取消・破棄・完了処理とは別。
- 将来の構想・機能候補：デバッグHUD、入力保存時のスケジュール問題の事前案内。後者は必須の予約受付検証・重複警告とは別で、採用未確定。
- 未採用・未確定：区間・距離併用の最終方式、弧長補正の具体実装、固定現示配列、全時刻表命令への対応。調査案を承認仕様に格上げしない。
- 整理2にある初期範囲から外す候補や低優先度案は、それだけで除外決定ではない。SpeakerController等を含むリリース範囲の全体確定は別途必要。

### 14.5 残作業の種類と完了の見方

- **利用者の判断が必要**：時刻表の対応範囲、編集・操作権、全異常時動作、初期範囲、品質の優先順位。
- **調査・設計が必要**：停止基準点、Createとの接続、共通進路・占有更新、曲線距離と探索負荷、制動数式、保存・同期方式。
- **全体整理後の数値決定**：プロファイル性能、EB移行時間、停止許容差・閾値、現示速度、進路期限・待機救済、更新間隔・探索上限・性能予算。通常移行10tickは既決定の基本値。
- **正式文書化が必要**：運転状態・遷移、全入力競合、制限統合、保存データ、設定、異常系、試験・受入条件。既存資料の棚卸し・分類後に移行する。
- **実装・検証は別工程**：今回の保存では行わない。仕様の基本方針が多数決まっていても、未決定事項を推測して実装開始できる状態とは扱わない。

## 15. 次の会話への引継ぎ

### 再開地点

最新の利用者依頼は、レバーサーを前進・中立・後進とし、滑らかなHUDレバーアニメーションと内部指定値を伴う自動切替を認め、これまでの会話を保存するもの。手動HUD終了で最大常用ブレーキとなる際は指定ノッチをNへ戻すが時点は未定。詳細は第11.10節を再開地点とする。

手動レバーサーの走行中切替可否・動作は利用者が調査中。直前の「停止かつNだけ受付」という提案への承認は得られていない。調査結果を待つ間、未承認条件を確定しない。中立の作用範囲、ATO等で自動レバー変更を除外する条件も未決定。

第8章の時刻表・予約・切替・通知、第10章の名前指定・候補選択、第11章の共通キー・占有は会話の最新方針へ更新済み。古いキー所有者通知、個別駅ID指定、全台HUD一括占有、予約の再確認保持などを復活させない。

進路の自動抽出方式は変更可能な暫定方針である。根元から完全に消失した分岐は表示から外すことを決定した。明示的削除・名前変更等の管理機能は将来検討のままである。ATOSの自動ダイヤ調整も将来候補で、現段階の自律操作は承認していない。

### 次に扱う候補（提案）

1. 利用者のレバーサー調査結果を受け、第11.10節の中立・走行中操作・自動レバー変更の適用場面と時点を整理する。最優先は終了時最大制動と指定Nの両立、ATO中の表示例外。
2. 共通キー・操作担当・閲覧・主副・切断・安全装置・ATOの状態表を完成させる。遠隔操作との競合、認可、対象・版の識別は未決定。
3. TASCの具体的な停止基準点・開始条件・OFF時出力引継ぎ、ATOの停車完了と時刻表進行を整理する。
4. 共通進路・占有のCreateへの接続、競合範囲と先頭／後尾基準、後退・標準列車との共存をローカルソースで調査する。
5. 共通現示・車上制限・距離精度・探索負荷を検討し、区間・距離併用方式の最終採否を判断する。
6. 時刻表の全対応命令・時間以外の待機条件・時刻指定、コピーと車上正本、切替設定の永続構造・連鎖を整理する。保存・再読込・未読込、SpeakerController、対応環境・初期範囲・品質基準も残る。
7. 全体の動作整理後に性能・期限・優先度・TASC精度等の具体値をまとめて決め、正式文書と実装前の確認事項へ分配する。

数値の細分化を一問ずつ先に進めず、全体構造・責任・動作を先に整理する。ソース調査はローカルのvendorで行い、不足ソースは利用者へ依頼する。実装・長時間検証は別途明示的な依頼があるまで行わない。

### 保存時に明示しておく残課題

- この文書は会話の決定を保存したもので、未決定事項を実装者が推測して埋める許可ではない。
- 運転状態機械・制限統合API・全入力競合表・全保存データ・全異常系は未完成。
- コードに数学モデルがあること、制御経路に接続されていること、ゲーム内で検証されたことは別。外部ソースの静的調査、一部の数学的説明、利用者のSteam ’n’ Rails実機観察は、CAT実装の動作検証ではない。
- 書込み対象の実リポジトリは `C:/Products/Minecraft-CreateAddons/CreateAdvancedTrains`。会話環境に残る旧パス `C:/Products/CreateAdvancedTrains` は保存時点で存在しなかった。
