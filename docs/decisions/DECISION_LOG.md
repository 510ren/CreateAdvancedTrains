# 決定ログ

## 2026-09-09 — 固定開発環境

- **決定:** Minecraft 1.20.1、Forge 47.4.10、Create 6.0.8-289、Java 17、Gradle 8.8を固定する。
- **理由:** Create内部仕様とMixin対象を一意にし、検証結果の再現性を保つため。

## 2026-09-09 — 通常制御の速度介入

- **決定:** 通常CAT制御は `Train.speed` を直接変更せず、`Train.targetSpeed` と
  Create標準の `approachTargetSpeed()` を利用する。
- **理由:** CreateのNavigation・手動運転・tick順序との競合を最小化するため。

## 2026-09-09 — 制御対象と状態管理

- **決定:** CATの状態はTrain UUID単位で管理し、将来はCAT Controller Blockを搭載する
  列車だけを制御対象とする。
- **状態:** Block検出は未実装。Create 6.0.8上の正確なCarriage/Contraption探索は要調査。

## 2026-09-09 — 制動モデルの基本原則

- **決定:** BrakingCurveは最終目標速度ではなく、残距離と制動性能から最大許容速度を返す。
  最終目標速度は将来の統合担当が複数制約の最小値として決める。
- **決定:** 制動応答時間は10 tick（0.5秒）とし、ノッチ変更を受けた時点から実効加減速度を
  目標値へ線形に遷移させる。この応答則はPhase 5Aの試験用挙動と本実装の両方に用いる。
  10 tickの不感時間後に一括で切り替える方式は採用しない。速度依存テーブルの区間補間も
  線形補間とする。
- **決定:** 線形遷移中に新たなノッチ要求が来た場合、切替直前の実効加減速度を新しい遷移の
  開始値として、10 tickで新しい目標値へ再遷移する。開始値へ戻すことや、以前の遷移を
  完走させることはしない。
- **決定:** `aTarget` は毎tick、現在速度で速度依存Profileを再評価する。速度変化だけでは
  10 tickの遷移をやり直さず、新しいノッチ要求が来た場合だけ再遷移規則を適用する。
- **状態:** B1〜B7・EBの具体性能、速度表、安全係数、逆算方法は未確定。

## 2026-09-09 — Phase 5Aの試験用常用制動ノッチ

- **決定:** B1〜B7はPhase 5Aの測定用に、Create `train.acceleration()`を基準とする
  1.0、1.5、2.0、2.5、3.0、3.5、4.0倍の暫定倍率を用いる。
- **決定:** Phase 5Aでは`FIXED_FOR_TEST`で一つのBノッチを固定し、ノッチ性能を
  自動選択の影響から分離して測定する。
- **決定:** 最終自動選択は、ATOから渡された要求減速度に対して最小の十分なBノッチを
  選ぶ責務として分離する。B7で不足する場合のEB発動はNotchSelectorの責務にしない。
- **状態:** 暫定倍率は試験後に差し替えるデータであり、最終B7曲線・倍率・個別補正ではない。
  詳細は `docs/design/NOTCH.md` を正本とする。

## 2026-09-09 — 受動型走行データ計測

- **決定:** Phase 5A前に、CATが制御値を変更しないCreate標準走行を記録する。
- **決定:** TrainDataDebuggerは独立した受動観測機能とし、設定有効時だけ列車別JSON Linesログを
  出力する。
- **記録先:** `docs/research/EXPERIMENTS.md`

## 2026-09-09 — Java実装と実行時試験の分離

- **決定:** Solの作業はJava実装、Create 6.0.8実ソースに基づく静的確認、Gradle buildに
  限定する。Minecraftの起動・操作、走行試験、ログ採取、実行時の受入判断はユーザーが
  手動で実施する。
- **結果:** Java実装依頼の完了条件に実行時試験を含めない。手動試験の手順・期待結果は
  仕様書へ記載できるが、実装完了報告とは分離する。

## 2026-09-09 — Phase 5Aの試験専用停止目標保持

- **決定:** 固定Bノッチの連続性能を測定するため、Phase 5Aの`FIXED_FOR_TEST`に限り、Create Navigationの
  初回native停止要求を契機に最終targetを0へ停止まで保持できる試験専用設定を追加する。
- **制約:** 既定は無効。`AUTO`、製品版ATO、TASC、EBには適用しない。`Train.speed`を直接変更しない。
  停止・無効化・列車/ワールド消滅・セッション終了で列車単位の保持状態を破棄する。
- **理由:** Create Navigationが基準加速度だけで制動距離を再計算するため、B7の強い外部制動に対して
  再加速targetを繰り返し出す。Profileの連続性能を測定するには持続的な減速要求が必要である。
- **結果:** 本実装の動的停止制御を置き換えない。将来はBrakingCurveとTargetSpeedResolverが、複数ノッチを
  切替えながら安全な動的targetを決定する。

## 2026-09-10 — 最終B1〜B7の等間隔Profile導出

- **決定:** 最終B1〜B7は、速度点ごとに等間隔の系列として扱う。B2〜B7の定常減速度を最小二乗で
  等間隔系列へ当てはめ、その外挿値からB1を求め、同じ系列で全ノッチを再構成する。
- **理由:** B1は10 tick応答によりCreate標準の停止開始地点で強制停車を受け得るため、強制停車を含む
  B1停止距離を個別性能の根拠にすると、日本の電車を基本とする等間隔ノッチ則を崩すため。
- **制約:** B1の実測は応答・定常性能・外挿値の妥当性確認に使う。強制停車を含むB1停止距離はフィットに
  使わない。外挿B1が非正、測定値との乖離が過大、または速度曲線が非物理的な場合は、追加測定を行う。
- **検証方法:** B1の定常減速度データは、B2〜B7だけで決めた仮B1 Profileに対する独立検証用データとする。
  B1データを先にフィットへ戻し入れて仮B1を自己検証することはしない。

## 2026-09-10 — Create列車情報照会の一元化

- **決定:** Navigationが利用できるときはCreate計算済みの`Navigation.distanceToDestination`を正本として
  利用し、手動運転でNavigation値がないときだけ、Create 6.0.8と同じstation探索から停車対象までの距離を
  導出する。
- **決定:** 距離、進行方向、停車に向けた減速かどうかなどのCreate関連判定は、単一の読み取り専用
  ユーティリティへ集約する。未取得距離を0で代用せず、HUDでは未取得として表示する。
- **工程:** このユーティリティ実装とSolのbuild成功報告を、B1〜B7の次の測定工程および制動数学へ進む前の
  必須前提とする。Minecraft実行時試験はユーザーが担当する。

## 2026-09-10 — 計算・定数の統一と可読性

- **決定:** 単位変換、再利用される計算式、Create照会、複数箇所で同じ意味を持つ判定は、責務・単位・
  不変条件が分かる統一クラスまたは専用値型へ集約することを検討する。
- **制約:** 一回限りで文脈が明白な計算まで機械的にクラス化しない。統一による再利用性・責務の一貫性・
  テスト容易性がない場合は、局所的に記述してよい。
- **決定:** 数値・比較式・単位変換の根拠を不明瞭にしない。マジックナンバーを残さず、名前付き定数、
  設定値、Profileデータ、意味を示す局所変数のいずれかで、単位・根拠・有効範囲を追跡可能にする。

## 2026-09-10 — 実在鉄道の快適停車を自動ノッチ選択の参照とする

- **決定:** 将来の駅停車時の自動ノッチ選択は、日本の電車およびATOの公開研究で確認できる、
  実減速度・予測停止位置のフィードバック、実効加減速度の連続性、停止へ向けた段階的な緩解という
  考え方を参照する。
- **決定しないこと:** 特定事業者・車種の操作手順、固定のノッチ通過順、ノッチ切替閾値、許容ジャーク、
  TASC引継ぎ条件は、この調査だけでは確定しない。
- **理由:** 同じノッチでも車両状態・粘着等で実減速度は変動し、実車の快適性も減速度とジャークの両方に
  左右される。固定列より、実測と予測を使う制御が安全性・停止精度・乗り心地の整合に適するため。
- **制約:** Phase 5Aの固定ノッチ測定、既決の10 tick線形応答、Solの実装開始条件は変更しない。具体的な
  選択規則は、Profileと制動数学の確定・ユーザー承認後に別途決定する。

## 2026-09-10 — 実車性能とゲーム内制動Profileの分離

- **決定:** 実在鉄道の車両性能・運転操作・ATO研究は、CATの設計における比較・参考資料として保存する。
  E235系など特定車両の数値を、ゲーム内のB1〜B7・EB Profileへ直接移植する要件にはしない。
- **決定:** ゲーム内の倍率、速度依存性、応答、停止距離、安全余裕は、Create上の測定データに加え、走行の
  楽しさ、制御の分かりやすさ、停車精度、安全性を優先して決める。
- **結果:** 実車の起動加速度と常用最大減速度から得られる比（例：E235系参照の`1.4`）は、有用な比較軸では
  あるが、採用必須の倍率ではない。

## 2026-09-10 — Phase 5Bの位置付け

- **決定:** Phase 5Bを、Phase 5Aの固定ノッチ測定の後に行う「常用制動数学と最終ゲーム内Profileの
  設計フェーズ」とする。Phase 5BはJava実装フェーズではない。
- **開始条件（更新）:** Phase 5Aの固定ノッチ測定が受入済みであり、手動運転時の距離未取得は
  Phase 9へ繰り越すというユーザー承認があること。手動探索の不具合はPhase 5Bを止めない。
- **完了条件:** 最終B1〜B7 Profileの根拠、応答を含む制動距離計算、残距離から最大許容速度を求める方法、
  安全側パラメータ、BrakingCurveの責務・入出力・受入条件を文書化し、ユーザーが仕様完成を明示承認すること。
- **制約:** この完了と明示承認まで、BrakingCurve、TargetSpeedResolver、製品版ノッチ自動選択、TASC、EBの
  Java実装を開始しない。

## 2026-09-10 — 手動距離未取得のPhase 5非阻害化とPhase 9 HUD方針

- **決定:** 手動運転時の停車対象距離が正しく取得できないことは、ユーザー承認によりPhase 5では完了扱いとし、
  Phase 5Bの制動数学設計を止めない。
- **決定:** Phase 9のCAT独立HUDでは、単一のNavigation destinationだけに依存せず、進行可能な近傍駅を
  おおむね4駅まで列挙して距離を表示する方向を採用する。
- **制約:** 正確な駅数、探索・分岐・並べ順、手動運転時の探索規則はPhase 9で決める。未取得距離を`0`で
  表現してはならない。

## 2026-09-10 — CAT独立操作系

- **決定:** CATの前進・後進は設定可能なCAT専用キーで切り替える。CAT独立操作が当該列車の操作入力を
  受け付ける間、従来の`W`/`S`による前後進操作は無効とする。
- **決定:** CATはCreate標準HUDおよび開発用Debug HUDから独立した運転HUDを提供する。
- **制約:** 初期キー、操作権限条件、入力優先順位、HUDのレイアウト・最終表示項目はPhase 9で決める。
  Clientは物理値を変更せず、Serverが入力意図を検証して既存の標準速度追従経路へ統合する。

## 2026-09-10 — 最終B1〜B7のB4基準化

- **決定:** 最終B1〜B7 Profileでは、B4を`baseAcceleration`に対応する基準ノッチとする。
- **決定:** `d_Bn(u) = baseAcceleration × {1 + (n - 4) × δ(u)}`とする。したがってB4は全速度で
  `baseAcceleration`に一致する。`δ(u)`は無次元の隣接ノッチ間倍率差であり、Phase 5Bでは定数関数
  `δ(u) = δ_0 = 1/5`とする。B1〜B7の倍率は`0.4`から`1.6`まで`0.2`刻みとなる。
- **決定:** 速度依存の`δ(u)`補正は、新設するPhase 10調整フェーズで、他機能を統合した走行データを
  根拠に行う。Javaは初期段階から`delta(u)`という関数境界を持つ。
- **制約:** Phase 5Aの`B1=1.0`から`B7=4.0`の暫定倍率は試験用であり、この決定により遡及変更しない。

## 2026-09-10 — 制動余裕、数値終端、計算エラーの初期方針

- **決定:** 全保護対象の距離から対象固有の保護余裕を引いた値の最小を`r_min`とし、CAT共通の安全余裕は
  `r_usable = r_min - M(u_current)`で別途引く。`M`は距離最小値の関数ではない。
- **決定:** Phase 5Bでは`M(u_current) = 10 blocks`を仮値とする。速度依存の余裕はPhase 10で統合走行データを
  根拠に決める。
- **決定:** 停止距離積分は`u < 0.001 blocks/s`で停止とみなし、最大反復回数は瞬時B7停止の基準tick数に
  10 tick応答と10 tickの異常検出余裕を加えた値とする。制御用速度は小数第4位で安全側へ丸め、表示だけは
  有効数字3桁へ四捨五入する。
- **決定:** 計算不能時は列車単位のCATエラー状態へ入り、マスコンへ原因を表示して自動運転を停止し、惰性運行と
  ユーザー通知を行う。原因解消後もレンチ操作による条件検証を経なければ復帰しない。

## 2026-09-10 — B7不足時のEB判断

- **決定:** 安全保護対象は全て対象位置の手前3 blocksを保護位置とし、B7安全側制動でこの位置を超えると
  予測される場合に上位安全機構がEBを発動する。
- **決定:** 駅停止は、B7持続でも停止位置を10 blocks以上超えると予測される場合だけEBを発動する。
  停車誤差±0.5 blocksを満たせないことだけではEBを発動しない。10 blocksはPhase 10で見直せる仮値である。
- **制約:** BrakingCurveは予測過走量・B7不足状態を返すだけで、EB状態変更や復旧処理を行わない。

## 2026-09-10 — 制動性能安全係数とNavigationオーバーラン

- **決定:** 制動性能安全係数`κ(u)`は将来の調整枠として関数境界を持つ。Phase 5Bは定数`κ(u) = 1.0`とし、
  Phase 10の比較候補は`0.95`程度とする。
- **決定:** `navigation.destination != null`かつ`distanceToDestination < 0`は、現在のdestinationに対する
  オーバーラン異常として脱線相当状態へ入れる。原因解消後のレンチ操作でのみ復旧する。
- **制約:** 経路途中の非destination駅を通過することはオーバーランではない。AからCへのNavigationでBを
  通過しても、残距離・HUD対象はCであり、異常扱いにしない。

## 2026-09-10 — Phase 5B Java実装の範囲と共有到着フラグ

- **決定:** Phase 5Bは、製品版NotchProfile/NotchResponseModel、BrakingCurve、Navigation残距離の正規化、
  到着共有フラグ、CAT計算エラー状態までを実装する。TargetSpeedResolver、製品版自動ノッチ選択、TASC、
  ATC/ATS/信号制約、実際のEB発動、マスコンGUI/HUDは実装しない。
- **決定:** BrakingCurveは`u_ceiling`を呼出側から引数で受ける。Phase 5Bでは製品呼出を接続しない。
  将来の統合担当が、CAT `maxSpeed`設定または承認済みのCreate上限を起点に、路線・信号・ATC等の制約後上限を渡す。
- **決定:** `AT_DESTINATION_OR_ARRIVAL_PENDING`は、Create Navigation状態からFlagDeterminerが導出する
  列車単位の共有・読み取り専用フラグとする。ATOControllerはこれを参照して新しい加速意図を抑制し、
  将来のTASC/ATC/信号も同じフラグを参照する。ATOControllerがフラグの真偽を任意に変更してはならない。
- **決定:** BrakingCurveの予測は、呼出時の`baseAcceleration`をスナップショットとして固定し、予測tick 0を
  `aStart`、tick 10をB7目標到達として扱う。問題が確認された場合は仕様変更を行う。

## 2026-09-10 — Phase 5Aの不要な通知・テスト上限の削除

- **決定:** `Phase5ANotchTestManager`の停止目標保持解除メッセージは削除する。
- **理由:** 当該判定関数は毎tick呼ばれ得るため、無効状態の通知を毎tick送信すると不要なクライアント通知と
  処理負荷を発生させる。解除の観測は既存のログ状態値を使用する。
- **決定:** `SpeedLimitSource.TEST = 20.0 blocks/s`の初期登録を削除する。
- **理由:** Phase 5Aの測定用上限としての役割を終え、通常動作へ残すべき製品設定ではないため。

## 2026-09-10 — CAT中立ノッチNとNavigation過走時の安全状態

- **決定:** 将来のCAT中立ノッチNは、`targetSpeed = 0`とノッチ制御の`accelerationMod = 0`を同じtickで
  組み合わせる状態とする。加速・減速を行わないノッチ状態は`accelerationMod = 0`で表す。
- **理由:** Create 6.0.8の`Train.approachTargetSpeed()`は、速度がtargetより大きいと標準加速度で減速する。
  従って`targetSpeed = 0`だけではNではなく停止要求になるため。
- **方針:** `destination != null && distanceToDestination < 0`で発生するCATのオーバーランは、Create物理脱線を
  呼ばず、独自の「脱線相当」安全ロックアウトイベントとして扱う方向とする。イベントの公開形、発火境界、
  通知、レンチ復旧との接続は、Forge/CATの実装可能性を確認して後続仕様で確定する。

## 2026-09-10 — Phase 5完了と後続Phaseの分割

- **決定:** Phase 5を完了扱いとする。完了範囲はPhase 5A固定ノッチ測定治具と、Phase 5Bの制動数学・
  製品基盤実装である。
- **制約:** この完了は自動ノッチ、TASC、信号、ATC/ATS、EB、マスコンの完成を意味しない。Minecraft上の
  手動走行試験は引き続きユーザーが受入判断を行う。
- **決定:** 後続は、Phase 6を統合速度制約・ATO・自動常用ノッチ、Phase 7をTASC、Phase 8を
  信号・ATC/ATS・EB・安全ロックアウト、Phase 9をマスコン・独立HUD・独立操作、Phase 10を
  統合走行試験に基づく調整として分割する。
- **正本:** 各Phaseの目的、対象、非対象、開始条件、受入条件は`docs/PHASE_ROADMAP.md`を正本とする。

## 2026-09-10 — Phase 6のCreate上限、P/N/B、ATO統括

- **決定:** Phase 6の`u_ceiling`はCAT独自設定を追加せず、制御呼出時の
  `abs(Train.maxSpeed()) × 20` blocks/sを用いる。燃料状態で変化するため毎呼出で再読出しする。
- **決定:** P1〜P5はP3を`baseAcceleration`の基準とする等間隔Profileとし、倍率を
  `0.6, 0.8, 1.0, 1.2, 1.4`とする。実車の絶対性能値は固定移植せず、Createのユーザー設定へ
  比率で追従する。
- **決定:** Nは0加速度の速度維持ではなく、走行抵抗相当の惰行減速とする。Phase 6の初期値は
  `-baseAcceleration / 6`であり、Phase 10の実走行データで調整する。
- **決定:** `V_safe = V × 0.95`、初期ヒステリシス`H = 0.5 blocks/s`を使い、10 tick応答込みの
  予測速度と段階的ノッチ遷移でP/N/Bの往復切替を防ぐ。Bの予測上限は`V_safe`と有効な
  BrakingCurve最大許容速度の最小値とする。通常はB4/B5を選び、不足時だけB6/B7を許可する。
- **決定:** Create境界では加速度修飾を非負に保ち、実効加減速度の符号で同一呼出のtarget方向を
  決める。P/N/B要求の切替で実効加減速度がゼロを跨ぐまで、target方向を早期反転しない。N定常時は
  `targetSpeed = 0`と`accelerationMod = 1/6`を当該呼出だけに適用する。
- **決定:** ATOControllerは通常CAT制御を統括するが、制約統合、ノッチ選択、応答の計算は
  TargetSpeedResolver等の専用クラスへ委譲する。既存の一回の`approachTargetSpeed()`を利用し、
  CATは追加呼出しをしない。
- **決定:** Phase 6ではCreateの目的地停止をBrakingCurveへ段階的に移管するが、Create信号停止と
  近駅補正は後続PhaseのCAT機能が完成するまで安全候補として維持する。Create標準手動入力は
  Phase 9の独自マスコン操作へ移管する。
- **制約:** Phase 5A測定治具を製品版制御へ流用しない。`Train.speed`、fuel、throttle、manualTick、
  Navigation destinationをCATが直接変更しない。
- **決定:** CAT適格性判定と独自マスコン統合はPhase 9まで実装しない。Phase 6は既存の
  `CONTROL_ENABLED`および`ATO_ENABLED`による有効化範囲を維持する。
- **決定:** 10 tick応答は列車UUIDごとにserver tick内で最大一回だけ進める。同tickの後続
  `approachTargetSpeed()`呼出はキャッシュ済みの実効加減速度を使い、より低いnative安全候補が
  現れた場合だけ当該呼出のfinal targetを安全側へ下げる。
- **決定:** 到着待ち、計算エラー、Navigationオーバーランでは、native targetとCreate元の
  `accelerationMod`を維持し、ノッチ応答を`SUSPENDED`とする。復帰後は古いノッチ状態を再開せず、
  実効加減速度0から新しい10 tick遷移を始める。
- **決定:** native target 0は、Createの信号待ち、Navigation目的地、Create標準手動入力、
  判定不能の順に分類する。目的地0だけは有効なBrakingCurve候補で置換できるが、それ以外と
  BrakingCurve不能時の0はCreate由来の安全候補として維持する。
- **決定:** Bを一段弱めるかは一段弱い候補の予測速度で判定する。`C = min(V_safe, BrakingCurve上限)`、
  `H = 0.5 blocks/s`として、`vPred(B(n-1)) <= C + H`で弱める。B4からは
  `vPred(N) <= C + H`のときだけNへ移る。Phase 6の自動B選択はB4〜B7に限定する。

## 2026-09-10 — CAT対象判定の組立時保存方針

- **決定:** CAT対象判定はCarriage Entityを毎tick走査して決めず、列車組立時または編成構成変更時に
  確定し、Train UUIDをキーとするCAT専用ワールド保存データへ保存する。読み込み範囲外でEntityが
  存在しない場合も、保存済み判定を使いCAT対象を誤って無効化しない。
- **決定:** 判定状態は少なくとも`CAT_ONLY`、`CREATE_ONLY`、`MIXED_CONTROLS`、
  `NO_VALID_CONTROLS`、`UNRESOLVED`を区別する。混在時はCreateを優先しCATを無効にする。
- **制約:** 独自マスコンをCreate組立要件の代替操作装置として統合する詳細、保存データの実装、HUD、
  独自入力はPhase 9で仕様化・実装する。Phase 6はこれらを先取りして実装しない。

## 2026-09-11 — Phase 6の駅停止をBrakingCurveから分離

- **決定:** Phase 6では、`GlobalStation`を目的地とする駅停止をBrakingCurveの速度候補で完遂しない。
  BrakingCurveは診断・安全監視用に計算してよいが、駅目的地についてはTargetSpeedResolverの最終速度候補から除外し、
  Create Navigation由来のnative targetを保持する。
- **理由:** Phase 5Bの共通安全余裕`M = 10 blocks`を駅までの残距離から直接引くと、駅手前で
  BrakingCurveが`u_max = 0`を返す。Resolverが最小値を採る既決則により、Createが要求する走行targetを
  上書きして列車が駅へ到着できなくなることを、Phase 6実走行ログで確認したため。
- **結果:** 駅位置±0.5 blocksの定位置停車、通常制動からの引継ぎ、TASC区間の低速制御はPhase 7の責務とする。
  この決定は、駅以外の安全保護対象に対するBrakingCurveの将来統合規則を変更しない。

## 2026-09-11 — TASCのクリープ到達条件

- **決定:** TASCは、停止位置までの正規化済み残距離が`1.0 blocks`に達するまでに、進行方向速度を
  `0.4 blocks/s`以下へ落とす。この条件は、停止位置±0.5 blocks・`v < 0.01 blocks/s`の最終停止判定へ
  入る前の必須条件とする。
- **理由:** BrakingCurveの共通安全余裕で駅手前に停止するのでなく、TASCが低速クリープから最終位置を
  補正するための明確な到達目標が必要であるため。

## 2026-09-11 — TASC無効時の駅停止

- **決定:** TASCは、自然な低速減速、クリープ、定位置停車を担当するCATの最終駅停止機能とする。
  TASC設定が無効な場合、CATはこれらの補正を行わず、Create標準のNavigation駅停止を維持する。
- **制約:** 無効化はNavigationや目的地を止めるものではない。また、Phase 6のBrakingCurveが駅目的地に
  対してtarget 0を保持してCreateの到着処理を阻害してはならない。設定キー、既定値、UIはPhase 7で決める。

## 2026-09-11 — Sol実装時のGradle build実行方針

- **決定:** SolはJavaソースの静的確認を必須とするが、Gradle buildはユーザーが明示的に要求した場合だけ実行する。
- **理由:** 実装ごとのbuild待機時間を通常手順へ含めず、必要な節目でユーザーが選択できるようにするため。
